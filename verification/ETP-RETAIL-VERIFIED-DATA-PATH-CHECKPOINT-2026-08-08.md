# ETP Retail Verified Data Path Checkpoint

Date: 2026-08-08
Starting HEAD: `9bf9653bb28b78e50b292fd1a81afbc8e5cf6c38`
Status: consolidated phase active; safe pre-production contracts implemented; production path gated

## Completed in the current uncommitted tranche

- ETP-A0 pure import policy and deterministic metadata identity.
- Bounded classic-ZIP preflight and decompressed OOXML threat inspection.
- Formula, entity/DTD, external relationship, hidden/ambiguous sheet, dimension, cell and shared-string refusal controls.
- Parser boundary that retains numeric lexical/type evidence and refuses numeric required identifiers.
- WLMHW aggregate structural benchmark for R022/R025/R013/R003.
- Generated API-23/Chrome-44 parser evaluation with generated assets removed afterward.
- Pure store/FY/period lifecycle, exact four-report manifest, staging/publication eligibility and restore-to-reimport fence.
- Explicit reconciliation-rule validator, scaled-integer aggregate/compare engine, coverage states and verified-view publication gate.
- One exact dictionary-compiled Retail profile for WLMHW and HEMW, with shared
  R003/R013/R022/R025 adapters, strict store isolation and real WLMHW history
  signature recognition.
- A pure import coordinator covering preflight through atomic publication,
  duplicate no-op, confirmation and previous-generation preservation.
- A pinned offline browser runtime connects four selected XLSX files through
  preflight, exact profile parsing, REC-002 reconciliation, encrypted staging
  and explicit confirmation. Its responsive shell UI exposes WLMHW/HEMW scope,
  four-file readiness, errors and verified status without showing business totals.
- Verified portable restore now fences every registered ETP scope and factory
  reset awaits deletion of the ETP store and key before reporting success.
- No parser, ETP policy or fact lifecycle module is loaded by the application runtime.
- A separate native `saagar-etp.db` infrastructure scaffold now supports exact
  scope isolation, bounded staging, native chunk hashing, sealing, atomic
  publication pointers, restore fencing and reset primitives. Its JavaScript
  adapter remains app-unloaded. Bounded dictionary-projected reads now
  authenticate every consumed chunk, but no application screen invokes them.

## Deliberately not started

- Production parser integration.
- Production-ready fact persistence: the approved Keystore AES-GCM envelope is
  implemented, but real API-23 crypto/tamper/crash evidence and runtime import
  gates remain open.
- Domain fact tables or any verified fact-reading API.
- Import UI or document-provider flow.
- R003/R013 production enrichment.
- E2–E6 verified dashboards or incentive money.
- Schema freeze, publication or owner acceptance.

Starting these now would activate an unverified data path while HEMW real-export
conformance and physical-device evidence remain missing. The shared schema is
owner-confirmed and compiled from the supplied Retail dictionary. The
at-rest encryption design is approved and implemented in the isolated scaffold;
its API-23 runtime evidence remains open.

## Native scaffold evidence

- Exact bridge/native lifecycle contract and scope-keyed active pointers.
- Exact R003/R013/R022/R025 allowlist; 500-row and 512 KiB chunk bounds.
- Native SHA-256 recomputation, fail-closed `quick_check`, structured transaction
  failure handling, restore fence, scoped reset and whole-store reset primitive.
- The complete ETP suite passed **115/115** after shared-profile, authenticated
  reads, browser runtime, UI, coordinator and recovery integration.
- API-23 emulator instrumentation passed **2/2** for Keystore round-trip, unique
  IVs, non-first-chunk tamper refusal, plaintext-canary absence, reset cleanup
  and unpublished-stage recovery. This is engineering evidence only.
- Modular architecture passed **72/72**, the complete repository suite passed
  **487/487**, and the debug APK built successfully with SHA-256
  `00EDCE3EC0EE093E01E5969EA57058F3F40554D34EAA554DFD6F28FED555302B`.
- The scaffold encrypts every chunk with a dedicated non-exportable Android
  Keystore AES-256 key, Keystore-generated IV, 128-bit GCM tag and versioned
  `ETP1` envelope. AAD binds scope, generation, report, chunk position, row count
  and plaintext digest. Full reset removes the database, sidecars and key alias.
- `finishStage` creates a constant-size authenticated generation seal binding
  scope, generation, manifest digest, row count and chunk count. Status verifies
  this seal without scanning fact chunks; future readers must authenticate each
  chunk they consume.
- It is not production-approved because fence-all restore wiring, verified fact
  reads and real API-23 crypto/crash/disk-full/corruption instrumentation remain
  open.

## Resume order

1. Close numeric-identifier handling and obtain HEMW conformance evidence.
2. Complete parser repeat/stress/memory and physical-device evidence.
3. Freeze both-store adapters and explicit R022/R025 rule.
4. Implement the separate native ETP store, atomic generation publication and import/re-import workflow.
5. Implement reconciliation/enrichment and E2–E6 read-only views.
6. Run recovery, privacy, store-isolation, API-23/device and owner acceptance.

The exact open-input register is `docs/ETP-RETAIL-VERIFIED-DATA-PATH-DECISION-REGISTER-2026-08-08.md`.
