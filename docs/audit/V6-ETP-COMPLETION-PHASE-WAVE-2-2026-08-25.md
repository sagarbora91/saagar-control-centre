# V6 ETP Completion Phase — Wave 2

Status: **ADAPTERS AND PRESENTATIONS IMPLEMENTED — GOVERNED GATEWAY NOT YET MOUNTED**

Wave 2 binds the canonical operational repository to the E3 and E4 orchestrator repository
contracts and supplies pure responsive presentations for both domains.

Implemented assets:

- `etp-operational-adapters.js` — E3/E4 repository adapters with conflict, isolation and restore
  fence enforcement;
- `etp-e3-presentation.js/.css` — accessible declaration, reconciliation, correction, disposition,
  lock, restatement and audit presentation;
- `etp-e4-presentation.js/.css` — prominent four-source readiness gate, immutable versions,
  allocations and verified pace presentation.

The presentations use injected governed facades and text-only DOM sinks. They have no native,
storage or parent-window authority. Role-dependent button visibility is usability only; the backend
grant remains authoritative.

## Deliberate activation boundary

These files are manifest-bound but not mounted in the ETP iframe yet. Wave 3 must build one
shell-owned operational gateway, register exact active E3 authority, expose a narrow bridge facade,
and mount E3. E4 may mount in blocked/readiness mode, but publish/revise/reallocate remain disabled
until all four approved E4 sources exist.

No E5 money path, Payroll mutation, HEMW production or E7 Service ETP is authorized.
