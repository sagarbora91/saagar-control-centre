{
  "counts": {
    "total": 55,
    "done": 3,
    "partial": 7,
    "open": 45,
    "out": 0,
    "overturned": 0
  },
  "plan": {
    "summary": "52 open P1 items sequenced into 7 build waves, each one file/owner so a module's base64 blob is only edited and re-encoded once. Start with Wave 7 (Front-desk: QMS + Service) — the highest-value, lowest-risk batch that puts recovered-sale rupees, collected-service revenue, and repeat-customer recognition on screen with zero carry-trap risk. Waves 8-10 harden the daily money modules (DSR, Stock+bridge, Expense). Wave 11 covers people-ops reports (grooming, CRO audit, payroll statutory). Wave 12 does leave-capacity + tax compliance. Wave 13 isolates the risky shell/security/report-engine work last. Two items dropped: P1-49 duplicates P1-37, and P1-55's remaining scope is a niche counter-sale add-on to already-shipped invoicing.",
    "recommended_first_wave": 7,
    "wave_first_item_ids": [
      "P1-1",
      "P1-9",
      "P1-4",
      "P1-54",
      "P1-42",
      "P1-11"
    ],
    "waves": [
      {
        "wave": 7,
        "title": "Front-desk cluster — QMS + Service",
        "theme": "Customer-lifecycle capture: recovered-sale value, walk-in fidelity, repeat-customer recognition, service collections & pickup notify",
        "item_ids": [
          "P1-1",
          "P1-4",
          "P1-54",
          "P1-42",
          "P1-9",
          "P1-11",
          "P1-10",
          "P1-2"
        ],
        "why_now": "Highest value-per-effort and lowest blast radius: both QMS (qms.html) and Service (service.html) round-trip whole-object JSON with no per-record normalizer to strip fields, so every new field survives reads with zero carry-trap work. Directly lights up recovered-sale rupees, collected-service revenue and repeat-customer signals owners cannot see today. Two adjacent front-desk blobs, each re-encoded once.",
        "est_effort": "Mixed S/M — mostly S (P1-1,4,54,9,11), M for P1-2 (Capacitor FS archive read), P1-10 (stageLog+TAT), P1-42 (cross-frame master lookup)",
        "target_files": "_extracted_modules/qms.html + _extracted_modules/service.html (re-encode into www/index.html); P1-2 exposes a read-only window.qmsArchiveLookup in index.html; P1-42 reads saagar_customer_master_v1 cross-frame via window.parent"
      },
      {
        "wave": 8,
        "title": "Daily Staff Register — dsr.html",
        "theme": "Point-of-sale capture, non-buyer follow-up pipeline, SM unlock-with-audit, WhatsApp EOD summary",
        "item_ids": [
          "P1-13",
          "P1-14",
          "P1-15",
          "P1-12"
        ],
        "why_now": "Single owner (dsr.html), re-encoded once, with the P1-13→P1-14 dependency satisfied in-order. P1-13 (mobile + payment mode) and P1-14 (non-purchase follow-up) share the normEntry/migrateRecord carry path (~L1490-1545) so migrate defaults are touched once. P1-12 (SM unlock-for-correction) also lives here and touches the same record shape plus the bridge merge/DSR_SUBMITTED re-emit — safest with the owner who just edited the schema. P1-15 (EOD WhatsApp, already 60% done) is a small renderDashboard button riding the same re-encode.",
        "est_effort": "S for P1-13/P1-15; M for P1-14/P1-12 (both med-risk: locked-record write-back + bridge re-emit)",
        "target_files": "_extracted_modules/dsr.html (re-encode); P1-15 also touches www/whatsapp-share.js + the DSR WA template in www/index.html; P1-12/P1-14 verify integration-bridge.js _mergeBridgeRows / DSR_SUBMITTED re-fire"
      },
      {
        "wave": 9,
        "title": "Stock Register + its bridge reads — stock.html + integration-bridge.js",
        "theme": "Lock/submit audit stamps, theft remark+SM-verify gate, monthly variance/shrinkage, sales reconciliation, and the read-only bridge exceptions",
        "item_ids": [
          "P1-5",
          "P1-7",
          "P1-6",
          "P1-8",
          "P1-40",
          "P1-43"
        ],
        "why_now": "One module owner (stock.html) plus the single file that reads its blobs (integration-bridge.js), opened once. All stock items share the normaliseImportData carry-trap (L2249-2285) so the new fields (lockedBy/submittedBy/reopenedCount/theftRemark/theftVerified) are added in one place. Order: P1-5 stamps → P1-7 adds the theft gate (med-risk, must not break theft==0 flow) → P1-6 shrinkage report consumes theft remarks → P1-8 reconciliation. Fold in the other read-only bridge pushes (P1-40 follow-ups-due, P1-43 cash cross-check) since buildExceptions is already open — bridge edited once, writes only saagar_exceptions (no wholesale-save race).",
        "est_effort": "S for P1-5/P1-8/P1-40; M for P1-6/P1-43; P1-7 med-risk (new Submit/Lock gate)",
        "target_files": "_extracted_modules/stock.html (re-encode) + www/integration-bridge.js (buildExceptions, _dsrRollup salesCount, theft exception)"
      },
      {
        "wave": 10,
        "title": "Expense Manager — expense.html",
        "theme": "Recurring templates, entry-time budget alerts, GST capture, and month-lock/stale-feed enforcement",
        "item_ids": [
          "P1-16",
          "P1-19",
          "P1-17",
          "P1-18"
        ],
        "why_now": "All four are the Expense module, one owner, one re-encode. They share the normEntry carry discipline (source/sourceRef already carried; add gstAmount/gstRatePct + tax-feed fingerprint) and the same addEntry/editEntry/voidEntry write path, so one owner reasons about the ledger holistically. Order: P1-16 + P1-19 (low-risk additive UI) → P1-17 (persisted GST fields + per-category rate map) → P1-18 last (med-risk write-path gate with owner-override-with-reason) so the lock guards the GST-enriched ledger. Bridge income-append path is explicitly out of the enforcement scope, so no cross-file scatter.",
        "est_effort": "S for P1-19; M for P1-16/P1-17; P1-18 S but med-risk (write-path enforcement)",
        "target_files": "_extracted_modules/expense.html (re-encode); reads saagar_master_vendors gstin at render time (no bridge edit)"
      },
      {
        "wave": 11,
        "title": "People-ops modules — Grooming + CRO Audit + Payroll statutory",
        "theme": "Grooming visibility (top-failed, store-split, checkedBy), CRO auto-fill + per-store targets + audit-pending, payroll statutory persistence & filing-ready outputs",
        "item_ids": [
          "P1-20",
          "P1-21",
          "P1-22",
          "P1-23",
          "P1-24",
          "P1-25",
          "P1-48",
          "P1-26",
          "P1-27"
        ],
        "why_now": "Three tight single-owner sub-clusters that never cross each other's blobs. Grooming (grooming.html, zero normalizers — plainly additive): P1-20 top-failed, P1-21 store dimension, P1-22 checkedBy. CRO Audit (cro_audit.html): P1-23 auto-fill bills from QMS feed, P1-24 per-store targets on cro_s_v3 (backward-compat byStore map), P1-25 audit-pending exception (one buildExceptions add). Payroll statutory (payroll.html + saagar-report.js): P1-48 first (persists statTotals the month-end pack reads), then P1-26 member-wise ECR/ESIC CSV and P1-27 F&F settlement builder. Larger wave but three self-contained owners; splitting would force re-opening the report engine and bridge again.",
        "est_effort": "S for P1-20/P1-21/P1-23/P1-48; M for P1-22/P1-24/P1-26/P1-27; P1-25 S (bridge read-only)",
        "target_files": "_extracted_modules/grooming.html, cro_audit.html, payroll.html (each re-encoded once) + www/integration-bridge.js (P1-25) + www/saagar-report.js (P1-26 PDF variant, P1-27 fnfSettlement builder)"
      },
      {
        "wave": 12,
        "title": "Leave Calendar + Payroll reconciliation + Tax module",
        "theme": "Per-store leave capacity & staffing strip & blackouts, payroll leave-source reconciliation, tax evidence/CA-pack/ZIP/QRMP & statutory card",
        "item_ids": [
          "P1-28",
          "P1-30",
          "P1-29",
          "P1-31",
          "P1-32",
          "P1-33",
          "P1-34",
          "P1-35",
          "P1-41"
        ],
        "why_now": "Two independent single-owner blobs batched. Leave (leave.html, one owner): P1-28 (per-store capacity — a one-line bridge reconcileMasters store-carry + new leavedesk_caps_v1 sibling key) lands before P1-30 (next-7-days strip, deps P1-28), then P1-29 (blackout sibling key). P1-31 (payroll reconciliation) is a read-only leavedesk_v3 month-sum swap inside payroll.html — self-contained. Tax (tax.html, one owner): P1-32 completeness map, P1-33 CA WhatsApp pack, P1-34 evidence ZIP (reuses shipped JSZip), P1-35 QRMP flag, plus P1-41 statutory-payable card — all share the taxcal_v2 record so one owner keeps its shape coherent.",
        "est_effort": "S for P1-30/P1-29/P1-33/P1-41; M for P1-28/P1-31/P1-32/P1-34/P1-35 (several med-risk: store carry, source-swap, QRMP visibility branch)",
        "target_files": "_extracted_modules/leave.html + www/integration-bridge.js (P1-28 store carry) + _extracted_modules/payroll.html (P1-31 read-only) + _extracted_modules/tax.html; P1-33 adds a WA_CFG.tax template in www/index.html"
      },
      {
        "wave": 13,
        "title": "Shell chrome + Reports/Backup + Security bridge channel",
        "theme": "i18n & attention-centre & text-size, rollback + report exports + weekly scope + lost-walkin sheet, then app-lock and the permission/PIN money-action channel",
        "item_ids": [
          "P1-36",
          "P1-38",
          "P1-39",
          "P1-44",
          "P1-45",
          "P1-46",
          "P1-47",
          "P1-37",
          "P1-50",
          "P1-51"
        ],
        "why_now": "Isolate the two highest-blast-radius plain-text files (index.html shell + saagar-report.js) for last, after every additive module change has landed and been proven. Shell chrome (P1-36 i18n, P1-38 attention dismiss/snooze, P1-39 text-size) hits the same Settings/render region. Reports/backup (P1-44 rollback, P1-45 lost-walkin sheet, P1-46 CSV export, P1-47 weekly scope) is one owner's saagar-report.js/index.html pass. Security ends the plan, prerequisite-first: P1-37 (app-launch PIN lock — must fail-open with no PIN, satisfies the dropped P1-49 too) → P1-50 (action-permission matrix + the NEW shell↔iframe request/response channel, L/high) → P1-51 (PIN-confirm on money actions, deps P1-50). Wiring the money-flow gates only after Waves 8-11 modules are stable minimises corruption of in-flight edits.",
        "est_effort": "M for P1-36/P1-38/P1-46/P1-47; S for P1-39/P1-44/P1-45/P1-37; P1-50 L/high-risk; P1-51 M/med-risk",
        "target_files": "www/index.html (i18n, attention, text-size, app-lock, permission matrix + editor, VERIFY_PIN channel) + www/saagar-report.js (CSV emitter, weekly builder, lostWalkinCallSheet); P1-50/P1-51 also blob-edit expense.html can() and payroll.html setRunStatus/lockRun to consult the shell verdict"
      }
    ],
    "out_of_scope": [
      {
        "id": "P1-49",
        "reason": "Duplicate of P1-37. Its own notes state it is 'the same deliverable as P1-37 — build once and satisfy both'; the auto-relock half is already shipped and verified, and the only remaining piece (app-open lock) IS P1-37, which is scheduled in Wave 13. Scheduling both would double-count the same build."
      },
      {
        "id": "P1-55",
        "reason": "PARTIAL and niche. The service-centre GST invoice is fully shipped and robust (Wave 3). The only residual is a generic counter-sale entry point reusing the existing serviceTaxInvoice builder off the existing saagar_invoice_seq_v1 counter — a nice-to-have generalization of already-shipped code, not an open gap. Most billing is franchise POS (explicitly out of scope), so it is lower-frequency than the revenue-capture items prioritised above. Revisit as a small follow-on once a host module for counter sales is settled."
      }
    ]
  },
  "done": [
    {
      "id": "P1-3",
      "name": "Lost-value KPI and winback list",
      "evidence": "qms.html:577 lostValueReport(d,w,m) (banner 'V6 Wave 1: Lost ₹ KPI') renders report-rows Today / Last 7 Days / Last 31 Days as ₹d.lost/₹w.lost/₹m.lost PLUS a 'Top Lost Leads · last 31 days' table (Customer, Mobile, Lost ₹, Reason) of the top-5 highest lostValue Non-Purchase closures. Backed by qms.html:461 calcStats() which computes lost = sum of (+c.lostValue||0) over outcome==='Non Purchase' and returns it. Wired into the Reports view at qms.html:568 renderReports() as a dedicated 'Lost ₹' card: `${lostValueReport(daily,weekly,monthly)}`. lostValue itself is captured at qms.html:557 confirmCloseLead (Object.assign c.lostValue). The winback template the item references also exists: index.html:4509 { id:'winback', name:'We missed you / offer', ok:c=>/non.?purchase/.test(c.outcome) }."
    },
    {
      "id": "P1-52",
      "name": "Udhaar (credit/khata) ledger with outstanding ageing",
      "evidence": "_extracted_modules/expense.html: getUdhaar/saveUdhaar (key K.udhaar='saagar_udhaar_ledger_v1'), addReceivable (line 1477), payReceivable with settlements[] part-payments (1489), udBucket ageing 0-30/31-60/60+ + udOverdue (1472-1473), renderUdhaar outstanding summary+ageing badges (1524-1564), remindReceivable one-tap WhatsApp via parent.shareText/wa.me (1514). Udhaar tab+pill registered (342/355/384/628). Isolated from gm_expenses by design."
    },
    {
      "id": "P1-53",
      "name": "Festival and wedding-season planner",
      "evidence": "_extracted_modules/planning.html (module 'planning', key 'saagar_festival_calendar_v1'): per-store targets titanworld/helios (saveFestival line 104-106), live QMS actuals via actualFor() reading retail_queue_management_v1 Purchase rows in [start,end] (82-87), pre-festival checklist addChkItem/toggleChk (122-124), blackout flag + 'Leave freeze' badge (56/143). Leave-gate hook: shell index.html lines 3239-3243 push a non-blocking 'Leave freeze' Attention item when a blackout festival is active or within 10 days, action openModule('planning'). Registered: index.html ACCESS_MODULES (2015), STORAGE_RULES planning (1968), validator planning() (2562)."
    }
  ],
  "buildable": [
    {
      "id": "P1-1",
      "name": "Capture recovered sale value when a follow-up is marked Converted",
      "area": "Queue Management System (QMS)",
      "status": "OPEN",
      "target": "blob",
      "target_detail": "qms module (_extracted_modules/qms.html): updateFollowup() + renderReports(); re-encode into www/index.html base64",
      "additive_safe": true,
      "effort": "S",
      "risk": "low",
      "deps": [],
      "missing": "On 'Converted': prompt for recovered amount (+ optional bill no.) and store additive fields on the followup record (e.g. f.recoveredValue, f.recoveredBill, f.convertedAt). Add a 'Follow-up recovered ₹' summary line to renderReports (sum f.recoveredValue over status==='Converted', with Today/7d/31d windows keyed on f.convertedAt/closedAt, mirroring lostValueReport).",
      "notes": "Additive-safe with zero carry-trap: load()/save() (qms.html:422) round-trip the whole state object as JSON — no per-record normEntry strips fields, so new followup fields survive reads. Prompt via existing openModal() (offline, no libs). Report windows should key on convertedAt (a new field) rather than dueDate. Optional: also show recovered ₹ per CRO in croPerformanceTable."
    },
    {
      "id": "P1-2",
      "name": "Archive lookup in Visit History",
      "area": "Queue Management System (QMS)",
      "status": "OPEN",
      "target": "multi",
      "target_detail": "shell www/index.html (expose read-only window.qmsArchiveLookup near lines 5715-5741) + blob qms.html showCustomerHistory()",
      "additive_safe": true,
      "effort": "M",
      "risk": "low",
      "deps": [],
      "missing": "(1) A shell-side read-only lookup exposed to the same-origin iframe, e.g. window.qmsArchiveLookup(mobile) reading saagar_qms_archive.json via fsPluginShell()/Capacitor Filesystem and returning matching records (no write). (2) An async 'Search older visits' button in showCustomerHistory that calls it (via window.parent.qmsArchiveLookup) and renders results below the live 6 visits; graceful no-op in browser preview (no FS).",
      "notes": "Read-only file access to an existing DATA file via the already-used fsPluginShell()/Capacitor Filesystem — NOT storage-core.js/sqlite-store.js/photo-store.js, so within the hard rules. Offline-only (local file). Cross-frame call uses the established window.parent bridge pattern (same as qmsWaMsgFor reading window.parent.WA_CFG at qms.html:564). Debounce the search so oninput history typing doesn't hammer the file; gate the button behind a check that Capacitor FS exists."
    },
    {
      "id": "P1-4",
      "name": "'No mobile given' walk-in capture",
      "area": "Queue Management System (QMS)",
      "status": "OPEN",
      "target": "blob",
      "target_detail": "qms module (_extracted_modules/qms.html): renderEntry() form + addCustomer(); re-encode into www/index.html base64",
      "additive_safe": true,
      "effort": "S",
      "risk": "low",
      "deps": [],
      "missing": "A 'No number given' checkbox on the entry form (renderEntry qms.html:539) that, when checked: disables/optionalises the mobile field, lets addCustomer() log the visit with an additive flag (e.g. c.noMobile=true, mobile:''), and SKIPS the /^\\d{10}$/ guard, the duplicate check, routeWithPreclaim's pre-claim match, and follow-up creation for that record. showCustomerHistory/contact buttons already no-op on empty mobile (qmsTel10 returns falsy), so those are safe.",
      "notes": "Additive-safe: new c.noMobile field survives the whole-object JSON round-trip (load/save qms.html:422); still counts in calcStats total/footfall (which counts all customers, not just those with mobile). Guard the branches: when noMobile, still require name; skip dedupe + routeWithPreclaim pre-claim lookup (call allocateCustomer directly) + block/disable the 'Follow-up Required=Yes' path (or allow follow-up only when a mobile exists, since qmsWaFu/contact need a number). Note the bridge harvests retail_queue_management_v1 into saagar_master_customers by mobile — a blank-mobile record is naturally skipped there, so no cross-module regression; confirm the bridge's customer harvester tolerates an empty mobile string without throwing during that integration pass."
    },
    {
      "id": "P1-5",
      "name": "Lock/submit audit stamps (who + when + re-open count)",
      "area": "Stock Register (daily brand-wise stock discipline, variance, locks)",
      "status": "OPEN",
      "target": "blob",
      "target_detail": "stock (V:/Co work/Projects/Retail/_extracted_modules/stock.html) — toggleLock, doLockClosing, submitMovements, reopenMovements, normaliseImportData, stMeta/print builders; actor name sourced from shell (index.html signed-in name)",
      "additive_safe": true,
      "effort": "S",
      "risk": "low",
      "deps": [],
      "missing": "Stamp lockedBy/lockedAt on opening+closing lock, submittedBy/submittedAt on movement submit, and increment reopenedCount on reopenMovements — persisted additively on the day blob and printed in stMeta()/register footers. The actor NAME is the gap: the module only knows st.mode (SM/CRO), not a person; the signed-in staff name lives in the shell (Wave-6 #28 named-staff attribution). Approach: read the shell's signed-in name via a read-only cross-frame lookup (same pattern the bridge/shell already expose for actor attribution) with a graceful 'SM'/'CRO' fallback, OR accept a lightweight name field. Add lockedBy/lockedAt/submittedBy/submittedAt/reopenedCount to normaliseImportData defaults so they survive import round-trips.",
      "notes": "Purely additive new optional fields on the existing day blob; no rename of openingLocked/closingLocked/movementsSubmitted, no record-shape change. MUST add the new fields to normaliseImportData (L2249-2280 brand loop is per-brand; the day-level booleans at L2283-2285) so re-import/backup round-trips carry them. Print side: extend stMeta() (L2471) to append lock/submit stamps. Register lists this as effort S. Low risk: no behavioural change to lock gating, just extra metadata + display."
    },
    {
      "id": "P1-6",
      "name": "Monthly variance & shrinkage report per store/brand",
      "area": "Stock Register (daily brand-wise stock discipline, variance, locks)",
      "status": "OPEN",
      "target": "blob",
      "target_detail": "stock (stock.html) — new monthly report builder reading saagar_stock_<store>_<date> keys directly for the month, printed via existing stPrintDoc (L2447); mirror the shell's L4070 month-key regex/loop pattern",
      "additive_safe": true,
      "effort": "M",
      "risk": "low",
      "deps": [],
      "missing": "A month-spanning report inside stock.html: loop every saagar_stock_<store>_<date> key for the selected month, build a brand x day variance grid (closingPhys - closingSys per counted day), sum theft/shrinkage per brand and per store, flag 'repeat offender' brands (variance/theft recurring across days), and render printable via stPrintDoc. Needs a month picker + store scope in the UI (module currently only has a single-date picker).",
      "notes": "Read-only aggregation over existing per-day blobs — no new storage keys, no schema change, no library. All inputs (closing display/storage/defective/yLoc, movements.theft, systemStock) already persisted. varClass/varText/totalPhys/closingSys helpers already exist in-module for per-day variance; reuse them across the month. Uses the same stPrintDoc/stHead/stMeta print rail as the four existing register builders. Effort M mainly for the month-grid layout + a month/store selector. Independent of P1-5 but shares the same closingSignoff/lock context — better after P1-7 lands theft-remarks so the shrinkage column can show reasons."
    },
    {
      "id": "P1-7",
      "name": "Theft entries require a remark and SM verify",
      "area": "Stock Register (daily brand-wise stock discipline, variance, locks)",
      "status": "OPEN",
      "target": "multi",
      "target_detail": "stock (stock.html — movements schema + submitMovements/doLockClosing gate + a theftRemark input) AND bridge (integration-bridge.js buildExceptions L510 — add a high-sev theft exception reading movements.theft from saagar_stock_<store>_<date>)",
      "additive_safe": true,
      "effort": "S",
      "risk": "med",
      "deps": [],
      "missing": "1) Require a non-empty remark when a brand's theft>0 (block Submit Day Movements / block closing lock until filled) — add an additive per-brand theftRemark field to the movements schema. 2) Require SM Verify on theft-bearing brands before closing lock (SM-only, mode==='sm'). 3) Push a high-severity 'shrinkage: <brand> theft <n> units' item into the Exceptions Hub — this second half is bridge-owned (buildExceptions), so it's a cross-file change.",
      "notes": "Additive: new optional theftRemark (and optional theftVerified) fields on movements[b]; MUST add them to the L2262-2268 normalise block + the L966/L2174 defaults so import/backup round-trips carry them (carry-trap). No rename of the existing theft field. Risk is med (not low) because it introduces a NEW gate on the Submit/Lock flow — must not break existing lock behaviour for theft==0 brands, and must respect roBlock()/past-view read-only guards already in submitMovements/doLockClosing. Bridge half is read-only (reads theft from the stock blob into buildExceptions) — consistent with how it already reads closingLocked at L527-528. Register effort S; the bridge exception push is the extra bit beyond a pure module change."
    },
    {
      "id": "P1-8",
      "name": "Sales units cross-check against DSR/QMS in Summary",
      "area": "Stock Register (daily brand-wise stock discipline, variance, locks)",
      "status": "OPEN",
      "target": "multi",
      "target_detail": "bridge (integration-bridge.js consumeDsrToStock L317 — add salesCount/salesAmt to _dsrRollup, mirroring the dsrSalesCount computation already at L424) AND stock (stock.html renderSummary L1473 + printSummaryReg L2599 — read _dsrRollup and render a 'Stock sales N vs DSR sales M — mismatch' line)",
      "additive_safe": true,
      "effort": "S",
      "risk": "low",
      "deps": [],
      "missing": "A reconciliation line in Summary comparing stock movements.sales (summed across brands per store) against the DSR sales for the same day/store, showing a 'mismatch' flag. IMPORTANT nuance: the _dsrRollup payload the bridge currently writes carries openingTotal/closingTotal/cros (DSR stock COUNTS), NOT a DSR sales count — sum() at bridge L323 sums r.opening/r.closing, not r.sales. So a true stock-sales-vs-DSR-sales line needs a DSR SALES figure that _dsrRollup does not yet contain. Two additive-safe options: (a) extend consumeDsrToStock to also stamp salesCount/salesAmt into _dsrRollup (the bridge already computes dsrSalesCount=r.sales.length + dsrSalesAmt for the CRO-audit feed at L424, so it's a column-copy), then read it in stock.html Summary; or (b) have stock.html read the saagar_dsr_<date>_<name> keys directly (r.sales.length) for the store/day. Option (a) is cleaner and keeps the module reading one aggregated key.",
      "notes": "Read-only display + one additive field on the already-additive _dsrRollup object — no rename, no existing-record-shape change. Bridge already has the DSR sales count logic (L424 sc=r.sales.length, sa=sum amount) so extending the rollup is a copy, not new math (as the register note claims). The module read is a pure display; if _dsrRollup is absent (older day / DSR not submitted) show nothing / 'no DSR data'. Effort S. Could be done module-only (option b) with zero bridge change if we prefer not to touch the bridge, but that duplicates the DSR-sales aggregation; recommend the bridge-adds-salesCount route. Independent of P1-5/6/7."
    },
    {
      "id": "P1-9",
      "name": "Collected-revenue figure (month) from final amounts",
      "area": "Watch Service Centre (service module — repair intake → delivery lifecycle, customer communication, charges)",
      "status": "OPEN",
      "target": "blob",
      "target_detail": "_extracted_modules/service.html: add a 'Collected (this month)' stat in renderDash() over DB.filter(status==='closed') summing parseRupee(c.delivery.finalAmt) where the close/collect month matches. Reads existing closed cases in saagar_wsf_v2; no new key.",
      "additive_safe": true,
      "effort": "S",
      "risk": "low",
      "deps": [],
      "missing": "No tile or report sums delivery.finalAmt for closed cases grouped by month (by closedAt/collectDate). Needs an additive month total, ideally a small per-month collected report block.",
      "notes": "Pure read-time aggregation over data already stored (delivery.finalAmt on closed cases). Reuse existing parseRupee(); bucket by closedAt (present) or delivery.collectDate; guard old cases where finalAmt is blank. Optional companion native PDF block in saagar-report.js (report target) but the tile alone satisfies the item. No storage-shape change."
    },
    {
      "id": "P1-10",
      "name": "Stage history timestamps + turnaround-time report",
      "area": "Watch Service Centre (service module — repair intake → delivery lifecycle, customer communication, charges)",
      "status": "OPEN",
      "target": "blob",
      "target_detail": "_extracted_modules/service.html: append to c.stageLog in quickStage() and the form stage-save path using the old-case-safe init pattern already used for followUps[] (`if(!Array.isArray(c.stageLog)) c.stageLog=[]`) and estimateApproval; 'by' from f-adv/advisor as in markEstimateApproved(). TAT report = new saagar-report.js block reading stageLog.",
      "additive_safe": true,
      "effort": "M",
      "risk": "low",
      "deps": [],
      "missing": "An additive stageLog[]={stage,at,by} appended on every stage change (in quickStage AND on the form's #f-stage save path), plus a turnaround-time report averaging received→ready / received→delivered, optionally per technician/brand.",
      "notes": "Cases persist as whole objects via saveDB() to saagar_wsf_v2, so a new c.stageLog array survives read round-trips (the CSV export at 3083-3126 is an export-only projection/whitelist and does not gate persistence). Old cases read as empty stageLog[] — same pattern as followUps[]/estimateApproval. Two write sites: quickStage() (card inline select) and the form stage select save. TAT report kept as a separate block so it degrades gracefully when stageLog empty on legacy cases."
    },
    {
      "id": "P1-11",
      "name": "One-tap customer notify when stage becomes 'Ready for Pickup'",
      "area": "Watch Service Centre (service module — repair intake → delivery lifecycle, customer communication, charges)",
      "status": "OPEN",
      "target": "blob",
      "target_detail": "_extracted_modules/service.html: add the notify offer in quickStage() when stage becomes 'ready', reusing waForCase()'s postMessage shape. Optional 1-line shell tweak in index.html openWAComposer to honor an incoming templateId for pre-selection; ST_WA handler already at index.html:6677 and the ST_WA_SENT loop already auto-logs via logWhatsAppFollowUp (service.html:3891-3904).",
      "additive_safe": true,
      "effort": "S",
      "risk": "low",
      "deps": [],
      "missing": "A hook in quickStage() (and the form stage-save path) that, when the new stage === 'ready', offers a one-tap 'Notify customer — Ready for pickup?' posting { type:'ST_WA', module:'service', recordId:id } (optionally templateId:'ready') so the shell opens the composer pre-selected on the existing 'ready' template.",
      "notes": "No new library, no storage change — offline wa.me composer already in place. On stage→ready, confirm() then postMessage ST_WA scoped to the case; the shell's existing round-trip logs the sent message into c.followUps via ST_WA_SENT, so accountability is captured for free. Guard with readOnlyGuard() (consistent with quickStage). Pre-selecting the 'ready' template, if desired, is a 1-line optional shell edge (secondary target: shell/index.html openWAComposer)."
    },
    {
      "id": "P1-12",
      "name": "SM 'Unlock for correction' with audit trail",
      "area": "Daily Staff Register (DSR / \"CRO Login\")",
      "status": "OPEN",
      "target": "blob",
      "target_detail": "dsr",
      "additive_safe": true,
      "effort": "M",
      "risk": "med",
      "deps": [],
      "missing": "A manager-gated 'Unlock for correction' action that clears rec.submitted (reopens the staff-side edit lock) while writing an additive unlock-audit stamp (who unlocked, timestamp, reason) so the correction path is logged. On re-submit, keep/append to the unlock log so the trail survives.",
      "notes": "Add e.g. rec.unlockLog = [] (array of {by, time, reason}) as a new optional field; add a manager-only 'Unlock for correction' button in renderSMAudit next to the reject/approve grid (gate to SM role + PIN reuse of the shell role-switch gate). On unlock set rec.submitted=false, rec.submitTime=null, push a log entry, and null any prior r.audit so it re-audits cleanly. normEntry/migrateRecord (~line 1490-1545) must carry unlockLog on read round-trips. Actor name should reuse the Wave-6 #28 signed-in-name attribution. Risk med because reopening a locked record touches the bridge merge path (_mergeBridgeRows line 1597) and the payroll/audit roll-ups keyed on submitted — verify the DSR_SUBMITTED bridge emit re-fires correctly on re-submit."
    },
    {
      "id": "P1-13",
      "name": "Customer mobile + payment mode on manual sales",
      "area": "Daily Staff Register (DSR / \"CRO Login\")",
      "status": "PARTIAL",
      "target": "blob",
      "target_detail": "dsr",
      "additive_safe": true,
      "effort": "S",
      "risk": "low",
      "deps": [],
      "missing": "Two inputs in showSaleModal: a Customer Mobile field (write entry.mobile) and a Payment Mode selector (Cash/UPI/Card -> entry.paymentMode). saveSale must read and persist both; the sales report/CSV rows (renderSMAudit ~line 2696, exportSingleCSV ~line 2980) should surface them.",
      "notes": "Add id=s-mobile (tel, reuse np-mobile's /^[0-9+\\-\\s]{6,15}$/ validation) and id=s-paymode (select Cash/UPI/Card) to showSaleModal. In saveSale include mobile + paymentMode in the entry object (Object.assign already carries them for edits). Additive optional fields — existing rows without them read as ''; no migration break. Feeds the festival/birthday customer list (Wave-3 Customer 360) and lets the owner reconcile cash-vs-UPI against Wave-5 expense byStore cash. Keep both optional (not required) so zero-friction for CROs who skip them."
    },
    {
      "id": "P1-14",
      "name": "Non-purchase follow-up pipeline",
      "area": "Daily Staff Register (DSR / \"CRO Login\")",
      "status": "OPEN",
      "target": "blob",
      "target_detail": "dsr",
      "additive_safe": true,
      "effort": "M",
      "risk": "med",
      "deps": [
        "P1-13"
      ],
      "missing": "A follow-up flag + date on nonpurch entries with a mobile, and a surfacing point: either a 'Yesterday's non-buyers to follow up' card on the next Day Start (renderDayStart), or a bridge feed into the QMS follow-ups tab. Each surfaced lead needs a wa.me/tel: action and a 'done/contacted' toggle.",
      "notes": "Add optional fields n.followUpDate (default = next day) and n.followedUp=false to nonpurch entries; migrateRecord must default them on read so old records survive. Surface in renderDayStart: scan prevDayStr(date,1..N) records for this staff/store, list nonpurch rows with a mobile and followedUp!==true, each with a wa.me link (reuse whatsapp-share.js patterns) + tel: link + a 'mark contacted' button that writes back followedUp. Cross-store reads use the same localStorage scan as computeDsrDay. Soft dep on P1-13 only in that richer sales-mobile capture makes the same-customer follow-up cleaner; can ship independently. Risk med: writing back followedUp onto a possibly-submitted prior-day record needs a guarded write (mirror _writeRecNow's read-only-date guard) so it doesn't reopen a locked record's other fields — write only the followedUp flag. Alternatively feed QMS follow-ups tab via integration-bridge.js (target=bridge) to avoid editing locked DSR records; that is the safer variant."
    },
    {
      "id": "P1-15",
      "name": "WhatsApp end-of-day DSR summary",
      "area": "Daily Staff Register (DSR / \"CRO Login\")",
      "status": "PARTIAL",
      "target": "shell",
      "target_detail": "www/whatsapp-share.js (+ DSR WA template in index.html ~line 4560; button hook in dsr.html renderDashboard)",
      "additive_safe": true,
      "effort": "S",
      "risk": "low",
      "deps": [],
      "missing": "(a) A one-tap 'Send day summary' button on the SM Dashboard itself; (b) per-store breakdown (present/submitted/pending/bills/total/top-CRO per store) — current template is single global sales line; (c) present-staff and submitted/pending counts in the message body (data already computed but not printed).",
      "notes": "Two-part: (1) enrich the existing index.html dsr WA template to include present/submitted/pending (already in computeDsrDay output) and a per-store split — either extend computeDsrDay to group by store (read r.store/staff->store map, additive) or add a computeDsrDayByStore helper; keep the old global template working. (2) Add a 'Send day summary' button in dsr.html:renderDashboard that builds the same text via a small window.SaagarWA-style call and opens wa.me (offline-safe). No new libs — whatsapp-share.js + wa.me only. Low risk, mostly additive string-building; the 60% already done is why this is PARTIAL not OPEN. If per-store store attribution isn't reliably on DSR records, ship the global-summary button first (S) and defer per-store to a follow-up."
    },
    {
      "id": "P1-16",
      "name": "Recurring expense templates (rent, salaries, utilities, insurance)",
      "area": "Expense Manager & Central Ledger (expense module: gm_* keys + tanishq_statements; plus integration-bridge CASH_CLOSED/voucher/GST wiring and saagar-report cashStatement/expenseMonthly PDFs)",
      "status": "OPEN",
      "target": "blob",
      "target_detail": "expense (module id 'expense'; new additive key e.g. K.recur='gm_recurring')",
      "additive_safe": true,
      "effort": "M",
      "risk": "low",
      "deps": [],
      "missing": "Whole feature: (1) a new optional 'gm_recurring' list of {id,type,category,mode,vendor,firm,store,defaultAmount,label,dayOfMonth,active}; (2) a Recurring sub-tab or a section in the ledger UI to manage it; (3) one-tap 'Post this month' that pre-fills the existing addEntry form (amount editable) and stamps source:'recurring'+sourceRef:<templateId>; (4) a per-month 'posted' guard (check ledger for an entry with that sourceRef+month) so a dashboard nudge can list unposted recurrences for CM.",
      "notes": "Fully additive and inside the HARD RULES — no lib, offline, new optional key only, and normEntry already carries source/sourceRef so posted rows round-trip cleanly. Reuse the existing EXP_CATS/MODES/storeOpts inputs. Nudge can live on the Dashboard render (CUR==='dashboard'). Do NOT auto-post (books-integrity) — always route through the editable addEntry form so the cashier confirms the amount. Keep templates owner/cashier-editable; no PIN gate needed."
    },
    {
      "id": "P1-17",
      "name": "GST capture: vendor GSTIN + GST-amount field on expense entries, per-category rate for the estimate",
      "area": "Expense Manager & Central Ledger (expense module: gm_* keys + tanishq_statements; plus integration-bridge CASH_CLOSED/voucher/GST wiring and saagar-report cashStatement/expenseMonthly PDFs)",
      "status": "OPEN",
      "target": "blob",
      "target_detail": "expense (module id 'expense'); also read bridge master gstin (saagar_master_vendors) at render time",
      "additive_safe": true,
      "effort": "M",
      "risk": "low",
      "deps": [],
      "missing": "(1) Capture/show vendor GSTIN: add a gstin input to addVendor + the vendors table, and persist {name,gstin} in gm_vendors (bridge already round-trips gstin, so this closes the loop); optionally prefill from saagar_master_vendors on match. (2) Add optional gstAmount (and optional gstRatePct) fields on ledger rows via a new normEntry key that defaults to 0/undefined so legacy rows survive. (3) Replace the single flat gstRate with an owner-configurable per-category rate map (e.g. set.gstRateByCat keyed on EXP_CATS/INC_CATS from L390-391) used in genTaxFeed's gstEstimate, falling back to the existing flat set.gstRate when a category has no rate. Keep the existing 'verify with CA' disclaimer text.",
      "notes": "Additive-safe: new optional normEntry fields must be CARRIED in normEntry so read round-trips don't drop them (same discipline as the Wave-5 store field at L495). gstEstimate shape in gm_tax_feed[CM] can gain an optional gstByCategory breakdown without breaking the bridge (buildTaxPayable L502-503 only reads .gstEstimate, so leave that key intact). No hardcoded tax law — all rates are owner-entered numbers. wa/report untouched unless you also want the breakup on the expenseMonthly PDF (optional follow-on)."
    },
    {
      "id": "P1-18",
      "name": "Enforce month lock + stale-tax-feed warning",
      "area": "Expense Manager & Central Ledger (expense module: gm_* keys + tanishq_statements; plus integration-bridge CASH_CLOSED/voucher/GST wiring and saagar-report cashStatement/expenseMonthly PDFs)",
      "status": "OPEN",
      "target": "blob",
      "target_detail": "expense (module id 'expense'); enforce in addEntry/editEntry/voidEntry + warn in renderClose",
      "additive_safe": true,
      "effort": "S",
      "risk": "med",
      "deps": [],
      "missing": "(1) In addEntry/editEntry/voidEntry, after the existing .closed check, also block when getStmt(date).monthLocked is true — with an owner override (can('reopen')/can('monthlock') both === 'owner' per L465/L1182) that requires a typed reason and writes an audit('month.override',...) entry. (2) Stale-feed warning: on generate, store a lightweight fingerprint (e.g. gm_tax_feed[CM].ledgerHash or a count+sum of non-void CM rows) alongside generatedAt; in renderClose, recompute and if it differs from the saved feed show a 'ledger changed after feed was generated — regenerate' banner next to the existing '✔ Tax feed generated' banner (L1410).",
      "notes": "risk=med only because enforcement changes write-path behaviour: must NOT block legitimate owner corrections (hence the owner-override-with-reason path) and must not trip on the bridge appending income into a locked month via IPC (bridge writes go straight to gm_expenses, bypassing addEntry — so enforcement lives only on the manual UI path, which is the intended scope). monthLocked field already exists and round-trips; the fingerprint is a new optional key on gm_tax_feed[CM], additive. Closes a real books-integrity hole feeding the Tax module/CA."
    },
    {
      "id": "P1-19",
      "name": "Budget alert at entry time + copy-last-month budgets",
      "area": "Expense Manager & Central Ledger (expense module: gm_* keys + tanishq_statements; plus integration-bridge CASH_CLOSED/voucher/GST wiring and saagar-report cashStatement/expenseMonthly PDFs)",
      "status": "OPEN",
      "target": "blob",
      "target_detail": "expense (module id 'expense'); addEntry post-save toast + renderBudget copy-forward button",
      "additive_safe": true,
      "effort": "S",
      "risk": "low",
      "deps": [],
      "missing": "(1) After a successful expense save in addEntry (L835-838, inside __save), compute the head's month-to-date actual vs gm_budgets[ym(date)][cat] and, if a budget exists, fire a second toast like 'Marketing now 104% of <month> budget' (only for type==='expense'). (2) Add a 'Copy last month's budgets' button in renderBudget that reads gm_budgets[prevMonthKey] and writes them into gm_budgets[currentMonthKey] for any head not already set (with an audit line), removing the re-keying that makes staff abandon budgets after month one.",
      "notes": "Purely additive UI on existing gm_budgets key — no schema change, no new key, no lib. Reuse the existing toast() + nINR() + ym() helpers. Copy-forward should not clobber a head the user already set this month (fill-only). Low risk; turns budgets from a report into a control exactly as the item asks."
    },
    {
      "id": "P1-20",
      "name": "Most-failed parameters report",
      "area": "Grooming Checklist (daily grooming discipline, scoring, floor-gate and gate-results linking)",
      "status": "OPEN",
      "target": "blob",
      "target_detail": "grooming (V:/Co work/Projects/Retail/_extracted_modules/grooming.html — renderMonthly + Month-End Report tab markup ~line 475-524)",
      "additive_safe": true,
      "effort": "S",
      "risk": "low",
      "deps": [],
      "missing": "A 'Top failed checks' panel: loop the month's records, tally items.filter(i=>!i.passed).label into a {label:count} map, sort desc, render a small list ('Shoes black & polished — failed 14×'). Read-only over data already stored; no schema change.",
      "notes": "Purely additive render-time aggregation using the existing items[] array. Add the panel inside the Month-End Report tab (renderMonthly). Escape labels via the existing esc() helper (line 810). Could also gender-split (items differ by cl-m/cl-f panels) but a flat label tally is enough for the purchasing-decision use case in the register. Consider adding the same to the CSV export (exportCSV line 1035) as a bonus."
    },
    {
      "id": "P1-21",
      "name": "Store-wise grooming view (WLMHW vs HEMW)",
      "area": "Grooming Checklist (daily grooming discipline, scoring, floor-gate and gate-results linking)",
      "status": "OPEN",
      "target": "blob",
      "target_detail": "grooming (grooming.html — saveCRO stamp + renderDaily/renderMonthly filter; reads existing key saagar_employee_master_v1)",
      "additive_safe": true,
      "effort": "S",
      "risk": "low",
      "deps": [],
      "missing": "At save time (saveCRO ~line 765), look up the matched employee via the existing grmEmployees()/name-match pattern and stamp optional store + employeeId onto the record. Old records read as undefined → bucket as 'Unassigned'/'—'. Add a store <select> filter to the Daily Summary and Month-End Report so Titan World (WLMHW) vs Helios (HEMW) can be compared separately.",
      "notes": "Fully additive optional fields — no existing key renamed, no record shape broken, so no normalize/migrate helper is even needed (grooming has none; records are plain arrays per day key). The employee-master read is already in-module (grmEmployees line 630), so the store value is one lookup away. Store labels in the master are 'Titan World'/'Helios' (STORE_OPTIONS index.html line 1999); the WLMHW/HEMW codes are the org-master branch codes (STORE_KEY_MAP line 1990) — display whichever the owner uses. Maps directly onto a store_id column in the PHP rebuild."
    },
    {
      "id": "P1-22",
      "name": "checkedBy accountability stamp",
      "area": "Grooming Checklist (daily grooming discipline, scoring, floor-gate and gate-results linking)",
      "status": "OPEN",
      "target": "blob",
      "target_detail": "grooming (grooming.html saveCRO — stamp checkedBy; renderDaily card — show it). Reads shell key saagar_active_staff_v1; optional tiny shell touch to also postMessage staff context.",
      "additive_safe": true,
      "effort": "M",
      "risk": "low",
      "deps": [],
      "missing": "Capture the checker into the record: checkedBy = (read saagar_active_staff_v1).name when a staffer is signed in; when the device is shared/nobody-signed-in, fall back to a lightweight one-time prompt/select for the SM's name (module has no role/admin awareness of its own today). Stamp checkedBy (+ optional checkedById) on the record in saveCRO ~line 765; show 'Checked by <name>' on each daily card (renderDaily ~line 839-851) escaped via esc().",
      "notes": "Additive optional field, same pattern as Wave-4 'attempt'. Effort is M (not S) because the module currently has ZERO checker/role concept, so besides the stamp you must add the identity source (read active-staff key) and a graceful fallback for the shared-device case, plus the card UI. This is anti-self-scoring: the register's concern is that CRO-role staff can self-score; recording who ran the check makes that visible even if the shell doesn't hard-block it (a full gate would be action-level permissions = P1-50, out of scope here). In the PHP rebuild make checked_by a required user id."
    },
    {
      "id": "P1-23",
      "name": "Auto-fill Bills/Invoices from QMS feed",
      "area": "CRO Daily Audit (in-app \"Store Manager\" module, id cro_audit)",
      "status": "OPEN",
      "target": "blob",
      "target_detail": "cro_audit (add autoFillBills near autoFillGrooming ~2059; hook into onCroDateChange ~2123; add an 'auto from QMS' note element beside #nps-bills built in buildNpsBody ~1061)",
      "additive_safe": true,
      "effort": "S",
      "risk": "low",
      "deps": [],
      "missing": "No code path copies croFeedEntry(date,cro).qmsSales into #nps-bills. Need: an autoFillBills() called from onCroDateChange() (and re-applied by loadAuditForEdit/buildTasks) that, on a feed hit, prefills #nps-bills with fe.qmsSales, keeps it editable (SM may override — spec says 'editable'), stamps an 'auto from QMS' note next to it (mirroring the .groom-note pattern), and calls onNpsInput() to recompute t5. Must NOT overwrite a value the SM has already typed for the current cro/date, and must clear/re-derive when the cro changes (same stale-value guard as autoFillGrooming's dataset.autoFilled handling), and must not clobber the SAVED billsCount during loadAuditForEdit (grooming already sequences itself LAST for this reason at 2187-2189).",
      "notes": "Purely read-time: reads existing saagar_cro_audit_feed[date][cro].qmsSales via existing croFeedEntry() helper (2009). No new key, no record-shape change (billsCount already stored in tData.t5 / audit.tasks.t5). Watch the loadAuditForEdit ordering (set bills BEFORE any feed auto-fill, or guard with a dataset flag like grooming does) so editing an old audit doesn't silently replace the saved billsCount with today's feed. Keep it editable + a visible 'auto from QMS' note so the response-rate denominator stays honest but overridable."
    },
    {
      "id": "P1-24",
      "name": "Per-store targets and store dimension",
      "area": "CRO Daily Audit (in-app \"Store Manager\" module, id cro_audit)",
      "status": "PARTIAL",
      "target": "multi",
      "target_detail": "cro_audit blob (getS→getS(store) at ~780, dashboard filter+selector in renderWeekDash/renderMonthDash ~1604/1690, saveSettings/loadSettingsForm ~782/797) + shell settings mirror index.html:2870 (cro_audit targets editor lists cro_s_v3 fields flat — keep the global as default; per-store editor lives in the module Settings tab)",
      "additive_safe": true,
      "effort": "M",
      "risk": "med",
      "deps": [],
      "missing": "Two additive pieces: (a) per-store target sets — extend cro_s_v3 so it can hold {byStore:{'Titan World, Latur':{...},'Helios, Latur':{...}}} while keeping the existing top-level keys as the default/fallback (getS(store) resolves byStore[store] over the global over DEF); add a store selector to the Settings target card so each store's 80/30/120 can differ. (b) a store selector on the Dashboard that filters cur/prv by a.store and passes getS(selectedStore) into the KPI math, so a small store is judged against its own target instead of the chain sum. (Records already carry store, so no migration of historical audits is needed.)",
      "notes": "HARD-RULE care: cro_s_v3 must stay backward-compatible — keep the existing flat keys (surveys/npsScore/rate/reviews/mktg) as the fallback and ADD an optional byStore map; getS() with no arg keeps returning the global so nothing else breaks. Note index.html:2870 has a shell-side generic editor for cro_s_v3 (flat fields) and index.html:1967 lists cro_s_v3 in the cro_audit reset/backup key set — both keep working since the extra byStore key rides inside the same blob. Default the Dashboard store selector to 'All' (current summed behaviour) so the change is purely additive. Medium effort because it touches Settings form, saveSettings, getS signature, and both dashboard renderers."
    },
    {
      "id": "P1-25",
      "name": "'Audit pending today' exception + reminder",
      "area": "CRO Daily Audit (in-app \"Store Manager\" module, id cro_audit)",
      "status": "OPEN",
      "target": "bridge",
      "target_detail": "integration-bridge.js buildExceptions() ~line 553 (add after the DSR block, mirroring its dsrClosingTime + active-roster-minus-leave pattern); optional home-chip red state in index.html:2624-2628",
      "additive_safe": true,
      "effort": "S",
      "risk": "low",
      "deps": [],
      "missing": "A read-only block inside buildExceptions that, in the evening (reuse the existing cfg().dsrClosingTime gate already used for the DSR flag at 545), computes active CROs from the employee master (minus leave-unavailable via GATE.unavailable, same predicate as the grooming block at 516-518) who have NO record in cro_audits_v3 for today, and pushes ex.push({sev:'high'|'med',area:'CRO Audit',msg:'N CRO(s) not audited today — ...',at:d}). Optionally upgrade the Home cro_audit chip (index.html:2624-2628) to an attention/red state when count is 0 after closing time. No storage change — reads cro_audits_v3 + EMP_MASTER + GATE, all already read by the bridge.",
      "notes": "Purely additive read: iterate cro_audits_v3 for a.date===today to build a done-set (keyed by kk(cro)), diff against active-non-leave CROs from EMP_MASTER (the grooming exception at 516-519 is the exact template — active roster minus GATE.unavailable[k].leave===true). Gate on cfg().dsrClosingTime like the DSR flag so it never false-fires mid-morning. Matching CRO identity: audits store the free-text cro name — normalise with the bridge's existing kk() helper to match master names. Writes only into the existing EXC blob via S(EXC,...) (already the sink for buildExceptions). Deep-link the exception/chip to cro_audit 'audit' target (universal-search entry exists at index.html:3488)."
    },
    {
      "id": "P1-26",
      "name": "Member-wise statutory register (filing-ready CSV/PDF)",
      "area": "Payroll Suite (payroll module in www/index.html + saagar-report.js payroll builders + integration-bridge.js payroll links)",
      "status": "OPEN",
      "target": "blob",
      "target_detail": "payroll module (decoded payroll.html; lives base64 in www/index.html). CSV is self-contained in the module (reuses appSaveBlob/payTotals/gmComputed). A PDF variant would add one builder to saagar-report.js + a META entry.",
      "additive_safe": true,
      "effort": "S",
      "risk": "low",
      "deps": [],
      "missing": "A per-employee statutory sheet. Needs: (1) a new member-wise CSV export function (e.g. reportStatutoryMemberCsv) that maps activeGmRows()→gmComputed to columns Name, UAN, ESIC IP, PF Wages, PF EE, PF Empr (pfEmpr), ESIC EE, ESIC Empr (esicEmpr), PT — matching PF-ECR / ESIC-contribution upload layouts; and (2) optionally a member-wise PDF register (either a new stPrintDoc table like the Salary Register, or a new saagar-report.js builder). No new math — it is column mapping over data already computed and stored.",
      "notes": "Additive read-only export — no storage-shape change (uan/esicIp already persisted; migration default '' at line 1570 so old rows read safe). Add a second button next to the existing CSV button (payroll.html:1170) reading 'Member-wise (ECR/ESIC)'. Keep the existing 'verify with accountant' disclaimer (payroll.html:1230). Simplest path is CSV-only (S); adding the PDF register too nudges toward M. P1-48 (persist statutory totals at lock) is adjacent but independent — the member-wise sheet reads live rows, so no hard dep."
    },
    {
      "id": "P1-27",
      "name": "Full & Final settlement document",
      "area": "Payroll Suite (payroll module in www/index.html + saagar-report.js payroll builders + integration-bridge.js payroll links)",
      "status": "OPEN",
      "target": "report",
      "target_detail": "saagar-report.js (new 'fnfSettlement' builder + META entry) driven by a new collector in the payroll module (decoded payroll.html HR tab, near hrLetterData ~line 2819 and renderHr ~2727)",
      "additive_safe": true,
      "effort": "M",
      "risk": "low",
      "deps": [],
      "missing": "A single F&F sheet that assembles: earnings (pro-rated last-month salary via calcGM + OT otAmount) + recoveries (advOutstanding, PT/PF/ESIC deductions) + net settlement figure + sign-off/acknowledgement — plus a UI action in the payroll HR tab to pick the exiting employee and generate it. Requires a new saagar-report.js builder (fnfSettlement) returning {orientation, blocks:[header, kv earnings, kv recoveries, kv net, para acknowledgement, sign]} + a META entry, and a payroll-side data-collector (like hrLetterData at payroll.html:2819) that computes the numbers and postMessages ST_REPORT.",
      "notes": "Additive: new builder + new module function; reads existing computed values (calcGM, advOutstanding, gmComputed) — no storage-shape change. Follow the proven hrLetter path exactly: payroll builds the data object, postMessages {type:'ST_REPORT',reportType:'fnfSettlement',opts} (pattern at payroll.html:2837), engine renders via para/kv/sign blocks. Effort is M (not S) because it needs a numeric earnings-vs-recoveries collector + net-settlement rounding, not just prose. Pairs naturally with the active-retire flag already shipped in Wave 4 (retire an employee → offer 'Generate F&F'). Keep 'verify with accountant' disclaimer consistent with statutory reports."
    },
    {
      "id": "P1-28",
      "name": "Per-store capacity view (WLMHW vs HEMW)",
      "area": "Staff Leave Calendar (LeaveDesk)",
      "status": "OPEN",
      "target": "multi",
      "target_detail": "blob=leave (extracted leave.html); bridge=integration-bridge.js:608",
      "additive_safe": true,
      "effort": "M",
      "risk": "med",
      "deps": [],
      "missing": "1) Carry store onto seeded Leave employees in bridge reconcileMasters() (add store:e.store||'' to the pushed object at :608 — additive field on existing record; a leave employee lacking store reads as ''/Unassigned). 2) In leave.html: add a Store select to the add-employee form + capture it in addEmployee() and onEmpIdSelect() (from opt.dataset.store). 3) Stamp store onto each leave record in submitLeave() from the chosen employee (employee.store), carried through coerceShape (old rows read store='' = legacy, counted chain-wide as today). 4) Make getCapacity() and MAX_DAY per-store: new sibling key leavedesk_caps_v1={perStore:{...},default:3}; split the day-modal capacity grid (leave.html:1580 capacityGrid) into per-store bars. HARD RULE: bridge must carry the new field so it survives re-seed round-trips; do NOT rename leavedesk_v3 or change existing record shape.",
      "notes": "Root cause is a one-line data drop in the bridge plus a missing dimension in getCapacity. The store enum ('Titan World'/'Helios') already lives in the central master; reuse those literal values. Existing leaves without store must keep counting (backward-compat) — treat missing store as an 'Unassigned' bucket that still consumes the global cap until re-stamped. This is the prerequisite data plumbing for P1-30's per-store strip."
    },
    {
      "id": "P1-29",
      "name": "Peak-season blackout dates + store holiday markers",
      "area": "Staff Leave Calendar (LeaveDesk)",
      "status": "OPEN",
      "target": "blob",
      "target_detail": "leave (extracted leave.html)",
      "additive_safe": true,
      "effort": "S",
      "risk": "low",
      "deps": [],
      "missing": "Add owner-configurable blackout ranges + store-holiday markers and a soft check in submitLeave(). Two additive options, both within HARD RULES: (a) reuse the agendas{} structure by adding a 'blackout'/'holiday' type (the item's suggested approach) — but submitLeave must then scan agendas for the leave's date; or (b) cleaner: a new sibling key leavedesk_blackouts_v1={ranges:[{from,to,label,store?}], holidays:[{date,label,store?}]} (mirrors the existing leavedesk_weekoff_v1 sibling-key pattern, leaves leavedesk_v3 shape untouched). On submitLeave, if a target date falls in a blackout, show a confirm()-gated override (same non-blocking override pattern already used for the entitlement-quota check at leave.html:1906-1909). Render a blackout/holiday marker on the calendar cell (renderCalendar ~leave.html:1409) and in the day modal.",
      "notes": "Effort S per register. Recommend the sibling-key approach over overloading agendas (keeps blackout config queryable and per-store without polluting the meeting/agenda list). Store-scoped holidays depend on the store dimension being available; can ship chain-wide first and add store filter after P1-28. Purely offline; no libs; reuses existing confirm()-override UX."
    },
    {
      "id": "P1-30",
      "name": "Next-7-days staffing strip",
      "area": "Staff Leave Calendar (LeaveDesk)",
      "status": "OPEN",
      "target": "blob",
      "target_detail": "leave (extracted leave.html)",
      "additive_safe": true,
      "effort": "S",
      "risk": "low",
      "deps": [
        "P1-28"
      ],
      "missing": "Add a read-only one-row strip above the calendar rendering the next 7 days from today: per day list who is on leave (name + FD/AM/PM from data.leaves[key], reusing leaveStatus/isActiveLeave and the existing type→label mapping). Split per store once P1-28 lands (needs the store stamp on leave records / employees to attribute each leave to a store). Pure render over data already in memory (data.leaves) — no storage change, no new key, no writes.",
      "notes": "Cheapest of the four (read-only view). Functionally standalone and could ship chain-wide immediately, but the item's spec ('...per store', 'Sat: 2 off') requires the store dimension, so it depends on P1-28 to be delivered as specified. Recommend building the strip now keyed off getAsOf()+6 days and layering the per-store columns after P1-28."
    },
    {
      "id": "P1-31",
      "name": "One-tap payroll reconciliation view",
      "area": "Staff Leave Calendar (LeaveDesk)",
      "status": "PARTIAL",
      "target": "multi",
      "target_detail": "blob=payroll (extracted payroll.html attFeedDiff/renderAttFeedPanel) reading leavedesk_v3; optionally a mirror view in leave.html",
      "additive_safe": true,
      "effort": "M",
      "risk": "med",
      "deps": [],
      "missing": "The diff+apply UX exists; what's missing is a leavedesk_v3-direct source so retro-entered leave reconciles. Add a read-time path that, for the selected payroll month, reads leavedesk_v3 directly (sum full=1/half=0.5 per employee-name, month-filtered, counted-leave only — reuse leaveStatus/isCountedLeave semantics) as an ALTERNATIVE/augment to loadAttFeed() in attFeedDiff(). Since payroll.html cannot call leave.html functions, re-implement the small month-sum over the leavedesk_v3 blob inside payroll (read-only; no write to leavedesk_v3). Optionally surface the same diff inside the Leave module. HARD RULES OK: read-only over an existing key, no schema change, no new lib, offline.",
      "notes": "Do NOT rebuild the reconciliation UI — it's done. The real work is source-swapping the leave portion of attFeedDiff() from the 7-day-lossy bridge feed to a direct leavedesk_v3 month-sum, so late/back-dated leave (the item's explicit failure mode and the main salary-dispute source) is captured. Keep DSR present-days from the bridge feed (Leave has no present-days). Reads cross-frame localStorage the same plain way modules already read leavedesk_v3. Effort M because of the careful name-matching (kk-style lowercasing, already the bridge's convention) and reconciling two sources without double counting."
    },
    {
      "id": "P1-32",
      "name": "Evidence-pack completeness tracking",
      "area": "Tax Compliance Calendar (tax module `taxcal_v2` + shell Reports/Action Center/bridge integration)",
      "status": "OPEN",
      "target": "blob",
      "target_detail": "tax module id 'tax' (_extracted_modules/tax.html); artifact->fileId tag map stored inside taxcal_v2 compliance record (additive)",
      "additive_safe": true,
      "effort": "M",
      "risk": "med",
      "deps": [],
      "missing": "1) A per-attachment 'expected artifact' tag (map an uploaded file to one line of profile.evidence). 2) A '2/3 evidence' completeness badge on the card comparing tagged files to profile.evidence.length. 3) A warn-on-Confirm when a High-risk item (profile.risk==='High') is closed with zero attached files AND empty evidenceRef.",
      "notes": "Additive-safe design: store the artifact tag INSIDE taxcal_v2 compliance record as an optional map {artifactRef -> [fileId,...]} keyed off SaagarEvidence file ids (no change to the shared file store); loadState/save path must CARRY the new field so it survives round-trips. Completeness badge = distinct tagged artifacts / profile.evidence.length; hydrates async like existing 📎 counts. Confirm-guard is a pure confirm() gate in confirmDone — trivial. Alternative (adding optional `artifact` to SaagarEvidence.add) is allowed since that store lives in index.html not storage-core, but touches the shared watch-photo store so keep it optional; prefer the taxcal_v2 map. This is what makes the dashboard 'audit readiness' claim true."
    },
    {
      "id": "P1-33",
      "name": "CA share pack per firm",
      "area": "Tax Compliance Calendar (tax module `taxcal_v2` + shell Reports/Action Center/bridge integration)",
      "status": "OPEN",
      "target": "whatsapp",
      "target_detail": "index.html WA_CFG.tax (new per-firm 'CA brief' template) reusing tax.html collectOutstanding()/exportComplianceCSV()",
      "additive_safe": true,
      "effort": "S",
      "risk": "low",
      "deps": [],
      "missing": "A per-firm WhatsApp template listing THIS firm's pending obligations (name · due date · days left · form/portal) plus the existing per-firm CSV attach, selectable in the tax WA composer.",
      "notes": "Lowest-code of the four. Best approach: build the message string INSIDE tax.html where collectOutstanding()+getActiveFirm() already exist (filter collectOutstanding to state.activeFirmId), then hand to the shell composer via existing WA plumbing / ST_SHARE; attach CSV from exportComplianceCSV(). Alt: add a build() to WA_CFG.tax that re-derives from taxcal_v2 filtered to activeFirmId (shell already parses taxcal_v2 in computeTaxStatus). wa.me only, offline-safe, pure read-time — no storage change."
    },
    {
      "id": "P1-34",
      "name": "Audit-ready evidence ZIP export (firm + FY)",
      "area": "Tax Compliance Calendar (tax module `taxcal_v2` + shell Reports/Action Center/bridge integration)",
      "status": "OPEN",
      "target": "blob",
      "target_detail": "tax module id 'tax' (button + JSZip build) calling parent SaagarEvidence.snapshotAll (index.html); save via ST_SHARE bridge",
      "additive_safe": true,
      "effort": "M",
      "risk": "med",
      "deps": [],
      "missing": "A 'Download evidence pack' action: filter SaagarEvidence.snapshotAll() to the active firm+FY (itemKey prefix firmId|FY|), zip files with JSZip, add index.csv (obligation, filed-on, ack/evidenceRef, file names joined from taxcal_v2 records), save/share firmname-FY.zip.",
      "notes": "No new library (JSZip already ships; used for payroll slip ZIPs). Pure read-only over existing SaagarEvidence blobs + taxcal_v2 — no storage-layer change (never touches storage-core/sqlite-store/photo-store). snapshotAll gives dataURLs; SaagarEvidence.dataURLToBlob (index.html:4965) converts for JSZip. Filter itemKey.startsWith(firmId+'|'+fyStartYear+'|'). index.csv joins each itemId back to STATE.compliance[firm][fy][itemId] for filedOn/evidenceRef + name. Route output through ST_SHARE (Android-safe) like exportComplianceCSV; sanitize firm name for filename. Synergy: if P1-32 ships tags, index.csv can note which artifact each file satisfies."
    },
    {
      "id": "P1-35",
      "name": "QRMP filing-frequency flag per firm",
      "area": "Tax Compliance Calendar (tax module `taxcal_v2` + shell Reports/Action Center/bridge integration)",
      "status": "OPEN",
      "target": "blob",
      "target_detail": "tax module id 'tax' (_extracted_modules/tax.html): ff-freq select in firm-modal, saveFirm/editFirm/resetFirmForm, getVisibleItems GST branch",
      "additive_safe": true,
      "effort": "M",
      "risk": "med",
      "deps": [],
      "missing": "1) A 'GST filing frequency: Monthly / QRMP' select on the firm form + additive firm.filingFreq field (default 'monthly' backward-compat). 2) getVisibleItems must show Monthly-filer GST built-ins when firm.filingFreq!=='qrmp' and QRMP-quarterly built-ins when ==='qrmp' (hide the other set).",
      "notes": "Additive: new optional firm.filingFreq (missing => 'monthly', preserving today's behaviour; never rename firm.type). Item's 'due dates from CA/spec not invented' constraint is SATISFIED — the QRMP GSTR-1(13th)/GSTR-3B(22nd)-labelled cards already ship in D with advocate-authored dates; task only swaps WHICH set renders. Tag built-ins monthly vs QRMP by name.includes('Monthly Filers') vs includes('QRMP') (or a small static id-set), gate in the getVisibleItems cat==='gst' branch alongside the existing type check; composition dealers still see CMP-08 unchanged. editFirm/resetFirmForm must set/clear the select so the round-trip carries filingFreq. IT/labour items unaffected."
    },
    {
      "id": "P1-36",
      "name": "Marathi/Hindi label toggle for shell chrome",
      "area": "Global UI/UX of the shell (Home dashboard, navigation, module launcher, FABs, theme, mobile/desktop layout, accessibility, vernacular)",
      "status": "OPEN",
      "target": "shell",
      "target_detail": "www/index.html (new LANG dict + t() helper + Settings Hub language card near the Display-mode card at line 1289); string table only, no module edits",
      "additive_safe": true,
      "effort": "M",
      "risk": "med",
      "deps": [],
      "missing": "Entire feature: language key, dictionary, t() helper, and re-pointing shell render sites through it, plus a Settings toggle.",
      "notes": "New per-device key (e.g. saagar_ui_lang; default 'en') alongside saagar_ui_mode; ~150-string dict {en,mr,hi} and a t(key) lookup used at render time for nav tabs, quick actions, attention titles/msgs, toasts. Modules live in base64 iframes and are OUT of scope — chrome-only per the item. Risk is breadth of touched render sites (must not break existing English literals) + Devanagari width in narrow mobile pills. Add a segmented card cloning the uiModeSeg pattern. Missing text-size toggle (P1-39) is independent; no hard dep but both land in the same Settings card region."
    },
    {
      "id": "P1-37",
      "name": "App-launch PIN lock (whole app)",
      "area": "Global UI/UX of the shell (Home dashboard, navigation, module launcher, FABs, theme, mobile/desktop layout, accessibility, vernacular)",
      "status": "OPEN",
      "target": "shell",
      "target_detail": "www/index.html (full-screen lock overlay shown before switchView('home') in init doFirstRender 6945; owner toggle in Settings 'Roles & access' subtab, line 1300)",
      "additive_safe": true,
      "effort": "M",
      "risk": "med",
      "deps": [],
      "missing": "Whole-app boot/resume lock overlay + owner opt-in toggle; only admin-mode auto-lock exists today, which does not protect non-admin data.",
      "notes": "Add opt-in key (e.g. saagar_launch_lock=true) + a styled modal (NOT prompt()) that overlays the app at boot and on resume-from-background, requiring ADMIN_PIN before revealing Home. Reuse verifyPin/pinHashV2 unchanged. Gate it in init before switchView('home'); also hook the existing visibilitychange/pagehide handlers (6929-6930) to re-show on resume. Distinct from SEC-PIN-07 which only auto-LOCKS admin mode, not the whole app. Risk: a bug here could brick boot for a non-technical owner — must fail-open if no PIN is set, and never persist a bypass token. Only available when hasAdminPin(); default OFF."
    },
    {
      "id": "P1-38",
      "name": "Attention Centre with dismiss/snooze",
      "area": "Global UI/UX of the shell (Home dashboard, navigation, module launcher, FABs, theme, mobile/desktop layout, accessibility, vernacular)",
      "status": "OPEN",
      "target": "shell",
      "target_detail": "www/index.html (renderHome attention block 3214-3269 + openAttention 3336 → convert to a modal listing all items with per-item done/remind controls)",
      "additive_safe": true,
      "effort": "M",
      "risk": "low",
      "deps": [],
      "missing": "Per-item acknowledge/snooze state + a real modal; today it is a static scroll to a 6-item list with no way to clear an item.",
      "notes": "Add a new key (e.g. saagar_attn_dismissed = {itemKey: untilIso}); give each attention item a stable key (derive from em+title, which are already deterministic per source). Filter items[] against the map at render; 'done' = far-future/permanent-until-condition-clears, 'remind tomorrow' = todayIso+1. Turn openAttention's scroll into (or add) a modal that lists ALL items (not just slice(0,6)) with done/snooze buttons. Keep the bell dot logic (3268) counting only non-dismissed items. Cross-module exceptions come from SaagarBridge.exceptions() (3250) — dismissal is shell-side only (never write back to the bridge). Additive; old installs read empty map = current behaviour."
    },
    {
      "id": "P1-39",
      "name": "Text-size setting (Normal / Large)",
      "area": "Global UI/UX of the shell (Home dashboard, navigation, module launcher, FABs, theme, mobile/desktop layout, accessibility, vernacular)",
      "status": "OPEN",
      "target": "shell",
      "target_detail": "www/index.html (new Normal/Large segmented card in Settings Hub beside Display mode at line 1289; root font-size / rem scaling)",
      "additive_safe": true,
      "effort": "S",
      "risk": "med",
      "deps": [],
      "missing": "The setting itself (key + Settings card) AND a scaling mechanism that actually works against the mostly-px CSS; nothing exists today.",
      "notes": "Add key (e.g. saagar_text_size = 'normal'|'large'; per-device, default normal) and a segmented card cloning the uiModeSeg markup (1291-1293) + setUiMode/reflectUiModeUI pattern (6884-6890). Apply by bumping the root font-size (html) or a --fs var. CAVEAT that raises risk from S-ish to med: the CSS is overwhelmingly px, not rem, so a single root bump will NOT enlarge most text — either (a) scope 'large' to a body/container transform or a coarse zoom on the shell chrome root, or (b) accept it enlarges only the rem/em-based elements. Chrome-only: the base64 module iframes won't inherit it unless the existing ST_UI_MODE postMessage pattern (6876) is extended, which the item scopes out. Verify no clipping of the fixed-height topbar/pills at Large."
    },
    {
      "id": "P1-40",
      "name": "Follow-ups due today on Home / Exceptions Hub",
      "area": "Cross-module workflow & integration (integration-bridge.js event bus + shell feeds)",
      "status": "OPEN",
      "target": "bridge",
      "target_detail": "integration-bridge.js — add a read-only block inside buildExceptions() reading QMS='retail_queue_management_v1' .followups, count status==='Pending' && dueDate<=today",
      "additive_safe": true,
      "effort": "S",
      "risk": "low",
      "deps": [],
      "missing": "",
      "notes": "In buildExceptions, after the QMS open-leads block (bridge:522-525), read q.followups (already have q=L(QMS,null)); count f where (f.status||'Pending')==='Pending' && String(f.dueDate).slice(0,10)<=d; push {sev:'med',area:'Follow-ups',msg:n+' follow-up(s) due today'}. The shell Attention list already renders saagar_exceptions items (index.html:3060,3251), so it surfaces automatically. Pure read; no QMS write (avoids the bridge-01 wholesale-save race). Optionally the home QMS chip can deep-link target 'followups'. Cite: openFollowups is already summed for a single mobile in customer360Lookup (index.html:3364,3376) proving the field is populated."
    },
    {
      "id": "P1-41",
      "name": "Statutory payable card in the Tax module",
      "area": "Cross-module workflow & integration (integration-bridge.js event bus + shell feeds)",
      "status": "PARTIAL",
      "target": "blob",
      "target_detail": "tax module (taxcal_v2) — add one summary card summing the current-month saagar_tax_payable rec {pf,esic,pt,gstEstimate} on the dashboard/calendar header",
      "additive_safe": true,
      "effort": "S",
      "risk": "low",
      "deps": [],
      "missing": "",
      "notes": "Read-time only; feed + parser (getTaxPayableFeed/taxPayableAmount) already exist. Add a card that reads getTaxPayableFeed()[ym] for the wages/supplies month and shows PF/ESIC/PT/GST line items + total, reusing the existing '· estimate — verify with CA' disclaimer string. No new storage, no bridge change. Down-scope from the register's 'S': most of the plumbing shipped in Wave 2 (W2-5b comment at tax.html:2581); only the aggregated presentation remains."
    },
    {
      "id": "P1-42",
      "name": "Repeat-customer recognition at QMS entry and Service intake",
      "area": "Cross-module workflow & integration (integration-bridge.js event bus + shell feeds)",
      "status": "OPEN",
      "target": "multi",
      "target_detail": "qms module (retail_queue_management_v1) entry form + service module (saagar_wsf_v2) intake form — both add a read-time byMobile lookup of saagar_customer_master_v1",
      "additive_safe": true,
      "effort": "M",
      "risk": "med",
      "deps": [],
      "missing": "",
      "notes": "Two blob edits, both pure read-time lookups on the existing bridge key (no writes to the master). QMS: extend showCustomerHistory() (qms.html:540) to also read window.parent localStorage saagar_customer_master_v1.byMobile[norm10(mobile)]; if found, show 'Repeat customer — N past visits, last bought X ₹Y' banner (source last-purchase from state.customers/master) and auto-preselect customerType='Repeat'. Service: in the intake form, on mobile blur, autofill custName from master names[0]. Risk=med because QMS runs in an iframe and its history box interacts with the live blob — must read cross-frame via window.parent.localStorage (same pattern qms.html:561 uses for window.parent.WA_CFG) and never write the QMS blob from a lookup. The customer360Lookup helper (index.html:3363) is a ready reference implementation to mirror."
    },
    {
      "id": "P1-43",
      "name": "Cash cross-check exception (QMS sales vs cash statement)",
      "area": "Cross-module workflow & integration (integration-bridge.js event bus + shell feeds)",
      "status": "OPEN",
      "target": "bridge",
      "target_detail": "integration-bridge.js — new read-only variance check in buildExceptions() comparing today's QMS cash-purchase total vs the day's statement cash income",
      "additive_safe": true,
      "effort": "M",
      "risk": "med",
      "deps": [],
      "missing": "",
      "notes": "In buildExceptions (or a small helper), for date d: (1) qmsCash = sum of q.customers where outcome==='Purchase' && paymentMode==='Cash' && closedAt/exitTime slice(0,10)===d of purchaseAmount; (2) stmtCash = from gm_expenses (EXP_LEDGER, already loaded at bridge:536) sum type==='income' && cash-mode && date===d, OR from tanishq_statements[d] cashIn. If both >0 and |diff| exceeds a configurable tolerance (reuse cfg() pattern like voucherThreshold at bridge:537), push {sev:'high'/'med',area:'Cash',msg:'QMS cash sales ₹X vs statement income ₹Y — mismatch'}. Pure read; writes only saagar_exceptions. Risk=med: (a) QMS Purchase amounts double-count with DSR-sourced income if the same sale is in both — scope to QMS-cash-only vs statement-cash-only and label as an advisory cross-check, tolerance-gated to avoid daily false red; (b) not every store bills cash through QMS (franchise POS), so make it opt-in via a cfg flag. Effort M for the reconciliation edge-cases, not the plumbing."
    },
    {
      "id": "P1-44",
      "name": "Undo last restore / module reset",
      "area": "Reports, PDFs, WhatsApp sharing, Backup & Data Safety",
      "status": "OPEN",
      "target": "shell",
      "target_detail": "www/index.html — Settings → Data & Backup panel near restoreValidatedBackup/resetSelectedModule (~lines 5405-5520)",
      "additive_safe": true,
      "effort": "S",
      "risk": "med",
      "deps": [],
      "missing": "A 'Rollback' UI card that lists existing st_v4_restore_rollback_* snapshots and an Apply button to replay one.",
      "notes": "Add a Rollback card scanning localStorage keys with RESTORE_ROLLBACK_PREFIX, list each (createdAt, module/title, key count) newest-first, Apply writes rollback.keys back via safeSet reusing the abort-on-partial-write guard in restoreValidatedBackup (5435-5453) then reloads. Only reads existing keys + writes back — no new key shape. Risk med (Apply overwrites live data): reuse same safeSet abort guard, auditLog('restore.rollback',...), take a fresh pre-rollback snapshot first, and gate behind the existing backup PIN (SEC #30)."
    },
    {
      "id": "P1-45",
      "name": "Lost walk-in follow-up list (weekly/monthly call sheet)",
      "area": "Reports, PDFs, WhatsApp sharing, Backup & Data Safety",
      "status": "OPEN",
      "target": "report",
      "target_detail": "www/saagar-report.js — new monthly report type e.g. lostWalkinCallSheet + META entry + hub wiring",
      "additive_safe": true,
      "effort": "S",
      "risk": "low",
      "deps": [],
      "missing": "A month-aggregated report listing non-purchase customers (name, mobile, reason, est. lost value) sorted by value.",
      "notes": "New monthly builder reading retail_queue_management_v1.customers[] (outcome ~ /non.?purchase/, month of entryTime) for {name,mobile,lostReason,lostValue,date} plus DSR nonpurch[] from saagar_dsr_* keys; merge, sort by lostValue desc, emit table with total. Register META scope 'monthly' + add to monthly hub. Reuse existing WA_CFG.qms 'winback' template (confirmed qms.html qmsWaMsgFor:564) for the paired one-tap call action if surfaced in-module. Read-only over existing keys — no storage change."
    },
    {
      "id": "P1-46",
      "name": "CSV/Excel export alongside PDF for register reports",
      "area": "Reports, PDFs, WhatsApp sharing, Backup & Data Safety",
      "status": "OPEN",
      "target": "report",
      "target_detail": "www/saagar-report.js — buildModel() table blocks + preview/pack Save bars (~lines 1398, 1513)",
      "additive_safe": true,
      "effort": "M",
      "risk": "low",
      "deps": [],
      "missing": "A CSV emitter that flattens report table blocks and a CSV button routed through the existing _saveBlob path.",
      "notes": "Add modelToCsv(type,opts) reusing buildModel to get blocks, flatten each {t:'table'} block (head+body+foot, section titles as label rows, blank line between tables), escape commas/quotes/newlines, build a text/csv Blob, route through the SAME _saveBlob (FS on device / anchor download on web, 1477-1490). Wire a CSV button next to Save in preview (1398) and pack (1513). No library (manual CSV). Prioritize register reports (payrollRegister, expenseMonthly, leaveRegister, stockRegister). Additive — generate/renderDoc/META untouched."
    },
    {
      "id": "P1-47",
      "name": "Weekly report scope + last-period comparison",
      "area": "Reports, PDFs, WhatsApp sharing, Backup & Data Safety",
      "status": "OPEN",
      "target": "report",
      "target_detail": "www/saagar-report.js — META add scope 'weekly' + new weeklyOwnerBrief builder + ownerMonthly delta columns + hub week selector",
      "additive_safe": true,
      "effort": "M",
      "risk": "med",
      "deps": [],
      "missing": "A weekly scope/report and 'vs previous month' delta columns on ownerMonthly.",
      "notes": "(a) Weekly: add weeklyOwnerBrief accepting a Mon-Sun range, loop 7 day-keys via existing computeQmsDay etc.; META entry scope 'weekly' + a week selector in hub (mirror hubStep +/-7). (b) Comparison: in ownerMonthly recompute prior month via the same compute* helpers for month-1 and add a delta column (value, prior, Δ%) to sales/conversion/expenses/payroll% KPIs. Risk med: prior-period must reuse existing month helpers (no new math) and show '—' when prior month empty. Additive — new scope value + new columns only; existing reports untouched."
    },
    {
      "id": "P1-48",
      "name": "Persist statutory totals at payroll lock so the Month-end pack includes them",
      "area": "Reports, PDFs, WhatsApp sharing, Backup & Data Safety",
      "status": "OPEN",
      "target": "blob",
      "target_detail": "_extracted_modules/payroll.html lockRun() (writes state.runs[k]) + saagar-report.js statutorySummary/shell bridge to read it",
      "additive_safe": true,
      "effort": "S",
      "risk": "low",
      "deps": [],
      "missing": "Persist the pre-summed statutory totals object onto the locked run, and let the month-end pack read it to build statutorySummary.",
      "notes": "In lockRun() (3045) also store statTotals=payTotals().T restricted to {emp,pt,pfEE,pfER,esEE,esER,net} onto state.runs[k]. This is an additive NEW field on the run record — ensure any run read/normalize path CARRIES statTotals across round-trips (unlockRun 3055 correctly rebuilds an 'approved' run dropping the snapshot, so statTotals rightly lives only on a locked run). Then either add statutorySummary to _packTypes.monthly with the shell report bridge reading state.runs[k].statTotals into opts.totals, or expose a getter. Manual openEngineReport path (1472) unchanged. Low risk, small, high value (only report with a legal remittance deadline)."
    },
    {
      "id": "P1-49",
      "name": "Admin auto-relock + optional app-open lock",
      "area": "Security, Roles & Multi-User",
      "status": "PARTIAL",
      "target": "shell",
      "target_detail": "www/index.html — new APP_LOCK_ENABLED_KEY + a styled lock-screen overlay shown at boot before doFirstRender, gated on verifyPin(); reuse existing pinHashV2/verifyPin/ADMIN_PIN_KEY and the visibilitychange/pagehide hooks already installed at 6928-6930 to re-lock on background.",
      "additive_safe": true,
      "effort": "S",
      "risk": "med",
      "deps": [
        "P1-37"
      ],
      "missing": "The owner-toggleable PIN-on-app-launch (whole-app lock screen) is not built. Only admin-mode auto-relock ships; the phone itself still opens straight to stock/cash/customer data with no gate on cold start. This is the same deliverable as P1-37 (App-launch PIN lock).",
      "notes": "Auto-relock portion is genuinely shipped and verified in code — mark that half done. Only the optional app-open lock remains; it duplicates P1-37 so build once and satisfy both. Risk med because a boot-time lock overlay sits on the critical first-paint path and a bug could soft-brick the app; must fail-open if no PIN set and must not touch storage-core. Styled modal, never prompt(), per the shell's own SEC-PIN posture. Reuse the ADMIN_PIN_KEY hash (or a separate launch PIN toggle) — no new key shape needed beyond one boolean enable flag."
    },
    {
      "id": "P1-50",
      "name": "Action-level permission matrix (approve / void / lock / reopen)",
      "area": "Security, Roles & Multi-User",
      "status": "OPEN",
      "target": "multi",
      "target_detail": "www/index.html (new ACTION_ACCESS_KEY map + renderer beside roleAccessMatrix + a request/response postMessage type in the 6669 handler) AND blob edits in expense.html can() + payroll.html setRunStatus/lockRun/unlockRun to consult the shell verdict; bridge is the transport (integration-bridge.js / shell postMessage).",
      "additive_safe": true,
      "effort": "L",
      "risk": "high",
      "deps": [],
      "missing": "One shell-owned role x ACTION permission map (e.g. approve/void/close/reopen/lock/monthlock) that modules consult via the postMessage bridge, plus an editor screen alongside the existing role matrix, plus each money-sensitive module action calling the shell to check it. Currently every action gate is per-module and inconsistent (expense uses can()+isSuperAdmin, payroll uses none).",
      "notes": "Largest of the three. Additive-safe as a new optional localStorage key (default = today's per-module behaviour so nothing tightens silently). Needs a synchronous or promised shell->module verdict channel: iframes are opaque-origin srcdoc so a module cannot read ROLE_ACCESS_KEY directly — must add a PIN/permission postMessage request/response pair (none exists today; handler at index.html 6669-6682 only has ST_AUDIT/ST_PRINT/etc). High risk: touches money-flow gates in two modules and changes the privilege boundary; a wrong default could either lock owners out or leave actions ungated. Build the shell map+editor first, then wire expense, then payroll. P1-51 depends on this same bridge plumbing, so land P1-50's request/response channel first."
    },
    {
      "id": "P1-51",
      "name": "Verified approvals: PIN-confirm on money actions",
      "area": "Security, Roles & Multi-User",
      "status": "OPEN",
      "target": "multi",
      "target_detail": "www/index.html (add a VERIFY_PIN request/response type to the 6669 message handler that calls verifyPin()/verifyStaffPin() and posts the boolean back) AND blob edits in expense.html approveEntry + closeDay/reopen and payroll.html setRunStatus/lockRun to await that verdict before committing.",
      "additive_safe": true,
      "effort": "M",
      "risk": "med",
      "deps": [
        "P1-50"
      ],
      "missing": "A shell PIN-verify request/response postMessage channel, and hooks in the fraud-sensitive module actions (expense approveEntry + cash close/reopen, payroll setRunStatus('approved')/lockRun) that require the admin (or the approver's personal staff) PIN before writing approvedBy/closedBy/lockedAt — turning those fields into trustworthy evidence.",
      "notes": "Reuses the exact same shell->module bridge that P1-50 needs, so sequence P1-50's request/response channel first (deps). verifyStaffPin (index.html 2191, Wave 6 #28) already exists so 'the approver's personal PIN' variant is cheap — the actor is activeStaff(). Additive: only reads existing PIN hashes, adds no record-shape change (approvedBy/closedBy already exist). Med risk because it sits on the approval write path — must fail-closed (block the action) if the PIN prompt is cancelled, and must not double-prompt when admin is already unlocked (offer 'already admin, confirm?' or still require PIN per the fraud-evidence intent). Styled modal, not prompt(), consistent with SEC-PIN posture. Can ship expense-only first, payroll second, to de-risk."
    },
    {
      "id": "P1-54",
      "name": "Birthday/anniversary WhatsApp greeting list",
      "area": "Retail domain best practice",
      "status": "OPEN",
      "target": "blob",
      "target_detail": "qms",
      "additive_safe": true,
      "effort": "S",
      "risk": "low",
      "deps": [],
      "missing": "",
      "notes": "Not built. Approach (additive): (1) add optional dob/anniv fields to the QMS walk-in form + customer record (normEntry/migrate must CARRY the new fields so they survive read round-trips); write them onto saagar_customer_master_v1.byMobile entries via the bridge harvest (or read-time). (2) Add a Home 'Greetings due today' card in index.html that scans the customer master for dob/anniv matching today's MM-DD and renders tap-to-send wa.me deep links (offline-safe, opens WhatsApp app). No new libs, no storage-core edits. Small: field capture + one Home card + wa.me link builder."
    },
    {
      "id": "P1-55",
      "name": "Native GST invoice/receipt PDF for service jobs and counter sales",
      "area": "Retail domain best practice",
      "status": "PARTIAL",
      "target": "report",
      "target_detail": "saagar-report.js (+ a host module, likely dsr or expense, for accessory counter sales)",
      "additive_safe": true,
      "effort": "M",
      "risk": "low",
      "deps": [],
      "missing": "",
      "notes": "Service-centre invoice is fully shipped and robust (Wave 3). The remaining scope is the item's second named target — accessory / non-franchise COUNTER sales (NOT Titan World franchise POS, which is explicitly out of scope). Approach: generalise serviceTaxInvoice into a shared native invoice builder that accepts a generic line-item + gst% + party payload, and add a small 'counter sale' invoice entry point in a suitable module (DSR manual sale or Expense income of type accessory) that stamps its own sequence off the existing saagar_invoice_seq_v1 counter. Additive: new optional invoice fields on the chosen sale record (carry through normalizers), no storage-core edits, reuse existing engine. Could be scoped to S if only reusing the builder for a minimal counter-sale flow."
    }
  ]
}