{
  "carry_fields": [
    "openingLockedBy: string '' (day blob top-level; P1-5; append after boolean block L2283-2285)",
    "openingLockedAt: string '' (ISO datetime; day blob top-level; P1-5)",
    "closingLockedBy: string '' (day blob top-level; P1-5)",
    "closingLockedAt: string '' (ISO datetime; day blob top-level; P1-5)",
    "movementsSubmittedBy: string '' (day blob top-level; P1-5; may legitimately be a CRO name)",
    "movementsSubmittedAt: string '' (ISO datetime; day blob top-level; P1-5)",
    "reopenedCount: number 0 (non-negative integer; sanitize via Number.isInteger(x)&&x>=0 else 0; day blob top-level; P1-5)",
    "reopenLog: array [] (day blob top-level; P1-5; sanitize each entry to {section:'opening'|'closing'|'movements' (else 'movements'), by:string '', role:'sm'|'cro' (else 'sm'), at:string '', reason:string ''}, drop non-objects, .slice(-50))",
    "theftVerified: {by:string, at:string}|null, default null (day blob TOP-LEVEL — P1-7 owns; shape-validate: object with string .by and string .at, else null; RESOLVED: P1-6's per-movements-row boolean reading is wrong and must be changed to read this day-level field)",
    "theftRemark: string '' (PER-MOVEMENTS-ROW — inside the wholesale rebuild literal at L2262-2268, add line: theftRemark: typeof m.theftRemark==='string'?m.theftRemark:'' — without this the import path silently DROPS it; P1-7)",
    "_dsrRollup / _dsrRollup.salesCount: DELIBERATELY NOT DEFAULTED (P1-8) — carried implicitly because normaliseImportData mutates parsed.data in place and never deletes unknown top-level keys (verified L2238-2287). HARD CONSTRAINT on the one-pass edit: keep the in-place pattern; NEVER 'tidy' the function into a rebuild-from-scratch of the top-level object or _dsrRollup (and any other unknown top-level key) is destroyed on import.",
    "P1-6, P1-40, P1-43: zero new persisted fields, zero carry entries. New localStorage keys across the whole wave: NONE."
  ],
  "stock_plan": "Single owner, one editing session on V:/Co work/Projects/Retail/_extracted_modules/stock.html. Cited line numbers are CURRENT-file; after Phase A every later anchor drifts — anchor by content/function name, not raw line number.\n\nPHASE A — P1-5 (audit stamps + audited re-open):\nA1. Insert stActor() + fmtStamp() after nowTime() (~L885). stActor() = read-only JSON peek at saagar_active_staff_v1 (.name, sliced 60) with fallback st.mode==='sm'?'SM':'CRO'. THIS IS THE SHARED ACTOR HELPER — P1-7's smName() is NOT implemented (see collisions #2); every stamp in the wave calls stActor().\nA2. initData() (L961-970): extend the fresh-blob literal with all Wave-9 day-level defaults IN ONE EDIT: the 8 P1-5 fields ('' / '' / 0 / []) PLUS P1-7's theftVerified:null. Wherever the movements-row literal is constructed (initData row template per P1-7's L966 cite, and addBrand() L2174), add theftRemark:''.\nA3. toggleLock() (L1856-1869): keep roBlock (L1857), SM gate (L1858), closing-lock detour (L1863) verbatim. Replace the flip at L1864-66 with: if (data[key]) { openReopenSheet(section); return; } — unlock is now audited; then lock leg sets data[key]=true + data[section+'LockedBy']=stActor() + data[section+'LockedAt']=ISO, saveData, lock-only toast, existing re-renders. (Covers opening lock + re-lock; closing lock stamps in A4 via doLockClosing.)\nA4. doLockClosing() (L1946-1981): insert the TWO stamp lines (closingLockedBy=stActor(); closingLockedAt=ISO) immediately ABOVE data.closingLocked=true (L1975). Leave closingSignoff (L1967-74) untouched. NOTE: P1-7's theft PRE-CHECK (Phase B7) lands EARLIER in this same function; both persist through the single existing saveData(data) at L1976.\nA5. submitMovements() (L1994-2004): add movementsSubmittedBy/At=stActor()/ISO immediately after data.movementsSubmitted=true (L1997). P1-7's gate (Phase B3) lands ABOVE this, between loadData and the flag — final composed body: roBlock → loadData → [P1-7 theft gate: missing-remark rows ⇒ openTheftSheet + return] → movementsSubmitted=true → P1-5 stamps → saveData → updateClosingTabState → renderMovementsHeader → toast → goTab timer.\nA6. New modal #stock-reopen-confirm after #stock-lock-confirm's closing </div> (after L800), ids rop-*; handlers openReopenSheet/ropValidate/reopenCancel/doReopenConfirm next to reopenMovements (~L2006), per P1-5 sketch (fresh loadData at confirm, roBlock+SM gates, reopenedCount++, reopenLog.push({section,by:stActor(),role:st.mode,at,reason}), cap slice(-50); lockedBy/At intentionally NOT cleared). reopenMovements() (L2006-2015) collapses to `function reopenMovements(){ openReopenSheet('movements'); }` — L549 onclick and applyReadOnlyChrome forceHide id (L1178) stay valid. Note doReopenConfirm never touches theftVerified — reopen without a theft edit keeps the stamp (P1-7 edge 4).\nA7. Summary/print/badge surfacing: add <div class=\"aud-strip\" id=\"sum-audit\"></div> after #sum-cards (L640) — FINAL DOM ORDER inside sec-summary: #sum-cards → #sum-dsr-recon (Phase D) → #sum-audit; renderSummaryAudit(data) per sketch, called in renderSummary() after the sum-cards innerHTML assignment (L1549-1559); clear #sum-audit in the empty-brands early return (L1479-85). printSummaryReg(): append the 'Audit Trail — Who Signed Off' stp-sec immediately BEFORE the stPrintDoc() call (L2659) — this stays the LAST body append (P1-8's parity line goes earlier, Phase D3). Badge title tooltips in refreshLockUI (after L1987) and renderMovementsHeader (after L2036). Add .aud-strip CSS near L137.\n\nPHASE B — P1-7 (theft remark + SM verify gates):\nB1. theftRows(data) helper before preLockClosing (~L1878) per sketch ((m.theftRemark||'').trim(), units = m.theft ?? 0, filter units>0). Do NOT add smName() — use stActor() (SM-gated call sites resolve to 'SM' fallback identically).\nB2. Markup: <div id=\"clk-theft\"></div> inside #stock-lock-confirm between #clk-summary (L792) and #clk-gaps (L793); new modal #stock-theft-confirm (ids thf-*) after Phase A's #stock-reopen-confirm — three sibling modals, id namespaces clk-/rop-/thf- disjoint.\nB3. submitMovements(): insert the theft PRE-CHECK per A5's composed body. doSubmitTheftRemarks() writes remarks then RE-CALLS submitMovements(), so the P1-5 stamps are written only on the clean pass — single submit path preserved.\nB4. openTheftSheet/thfValidate/theftSheetCancel/doSubmitTheftRemarks after submitMovements, per sketch (fresh loadData, roBlock, writes movements[brand].theftRemark only for rows that still exist).\nB5. openPreLockSheet() (L1893-1922): render the #clk-theft section after the gaps block (L1903-1918): empty when theftRows()===[]; verified banner when data.theftVerified.by; else red header + rows (legacy remark-less rows show 'No remark (recorded before this update)') + #clk-theft-cb checkbox wired to clkValidate.\nB6. clkValidate() (L1925-1938): compose theftPending = !!cb && !cb.checked into the disable/err logic per sketch (checkbox absent ⇒ byte-identical behaviour to today).\nB7. doLockClosing(): insert theft PRE-CHECK right after the missing-gap guard (L1955-58), BEFORE the gapRemarks loop and before ANY mutation: if theftRows nonempty and no valid stamp — checkbox unticked ⇒ set clk-err + return; ticked ⇒ data.theftVerified={by:stActor(),at:ISO}. Final doLockClosing order: roBlock → SM gate → fresh loadData → gap guard → P1-7 theft gate → gapRemarks loop → closingSignoff → P1-5 stamps → closingLocked=true → saveData → close/render/toast.\nB8. updMov() (insert after m[field]=newVal, L1695): if field==='theft' && prev!==newVal && data.theftVerified ⇒ null it + warn toast.\nB9. Surfacing one-liners: renderSummary theft scard sub (L1554, inside the sum-cards HTML string — coordinate with A7 which only APPENDS a call after that assignment, no overlap) and renderMovementsHeader submitted info text (L2039-41).\n\nPHASE C — P1-6 (Monthly tab, read-only):\nC1. Tab button tbtn-monthly after tbtn-summary (L478); goTab else-if renderMonthly() after L2079.\nC2. sec-monthly markup between sec-summary's closing </div> (L662) and the SETTINGS comment (L664) — exact block from the P1-6 spec.\nC3. Monthly JS after renderSummary's closing brace (L1561): _monMonth/_monN, monDaysIn/monLabel/monRawDay/monCellCalc/monAggregate/monSetMonth/monSetN/renderMonthly. ONE RECONCILIATION FIX (collisions #1): theftVerified is DAY-LEVEL {by,at}|null (P1-7 owns it), NOT a movements-row boolean — monCellCalc(brand, day) must return theftVerified: !!(day.theftVerified && day.theftVerified.by) instead of m.theftVerified===true; theftRemark stays per-row (m.theftRemark). Consequence: every theft row of a verified day shows '✓ SM verified' (see open questions). P1-6's harness seed must also move theftVerified to the day blob top level.\nC4. printMonthlyReg() after printSummaryReg (orig end L2660 — anchor to the function's closing brace by content since Phases A/D added lines inside it).\n\nPHASE D — P1-8 (DSR reconciliation line, stock side):\nD1. Insert <div id=\"sum-dsr-recon\" style=\"display:none\"></div> BETWEEN #sum-cards and Phase A's #sum-audit.\nD2. renderDsrRecon(data, tSales) next to renderSummary (per sketch: hide unless Number.isFinite(Number(_dsrRollup.salesCount))>=0; OK/mismatch/not-entered variants; only coerced numbers reach the DOM); call it as the LAST statement before applyReadOnlyChrome() (orig L1560); add the #sum-dsr-recon clear to the empty-brands early return alongside Phase A's #sum-audit clear.\nD3. printSummaryReg parity line after the stp-cards block (orig L2651) — final print body order: cards → P1-8 'Cross-check: …' line → existing brand tables → P1-5 'Audit Trail' section → stPrintDoc.\n\nPHASE E — THE ONE-PASS normaliseImportData EDIT (all items together, L2238-2287):\nE1. Inside the movements rebuild literal (L2262-2268): add theftRemark carry line (P1-7).\nE2. After the boolean block (L2283-2285): append P1-5's 8-field default/sanitize block, then P1-7's theftVerified shape-validate. Nothing for P1-6 (no fields) or P1-8 (salesCount deliberately absent — see carry list constraint: keep the in-place mutation pattern so _dsrRollup rides through).\n\nPHASE F — verify per each spec's harness section (P1-5 #9 and P1-7 #6 CARRY checks, P1-8 #8 _dsrRollup round-trip, P1-6 #5 zero-write snapshot), then re-embed into www/index.html via the scratchpad p1plan/embed.js pipeline, byte-exact (NEVER round-trip the shell through PowerShell Get/Set-Content — mojibake caution).",
  "bridge_plan": "Single owner, one editing session on V:/Co work/Projects/Retail/saagar-control-centre/www/integration-bridge.js (plain file — no embed step). Apply in this order; after edit 3a all later line numbers drift, so anchor by the existing comment strings.\n\n1. P1-8 helpers — insert dsrStoreCode(r) and dsrDaySales(date,code) near dsrKey() (~L196), per the P1-8 sketch. dsrStoreCode: r.store||r.storeCode when 'WLMHW'/'HEMW' (never set today), else EMP_MASTER lookup by kk(staffName) mapping store text containing 'helios'→HEMW / 'titan'→WLMHW, else 'WLMHW'. dsrDaySales: full recompute over ALL localStorage keys prefixed 'saagar_dsr_'+date+'_' with submitted===true resolving to the target store, counting sales[] rows where type!=='service' && Number(amount)>0. Never an increment — idempotent under P1-12 :v<rev> re-submits.\n\n2. P1-8 consumeDsrToStock (L317-332) — two changes, verified against current file: L322 `var st=(r.store||r.storeCode||'WLMHW').toUpperCase(), b=agg[st]||agg.WLMHW;` becomes `var b=agg[dsrStoreCode(r)];` (resolver returns only WLMHW|HEMW, both seeded at L318, so the ||agg.WLMHW guard is preserved by construction); the L329 rollup literal gains ONE field `salesCount:dsrDaySales(d,sc)` — everything else byte-identical. The write remains the existing single S(sk,sb) at L330; the scan runs only inside if(touched).\n\n3. buildExceptions (L524-571) — three inserts, top-to-bottom so each anchor is edited before it drifts. This function's ONLY localStorage write stays S(EXC,{date:d,items:ex,generatedAt}) at L569 — none of the three blocks add a write (hard rule: saagar_exceptions only).\n  3a. P1-40 — REPLACE the 'QMS open leads today' block (L535-539) with the spec's version: open-leads logic byte-identical, followups count added as a SIBLING if(q&&Array.isArray(q.followups)) inside the SAME try (reuses parsed q; a legacy blob without customers still gets followups counted — note the try's closing }catch(e){} moves below the new if). Item: {sev:'med',area:'QMS',msg:fuDue+' follow-up(s) due'+(fuOver?' ('+fuOver+' overdue)':'')+' — see Follow-ups',at:d}. Predicate mirrors qms navBadgeFu (status==='Pending' && dueDate<=d, lexicographic, overdue included; non-string/empty dueDate excluded). COPY BAN (forever): msg must never match /open lead/i — buildCloseDaySteps (index.html L3070) regex-filters area-QMS items for the EOD wizard. Adds ~18 lines.\n  3b. P1-7 — insert the unverified-theft block AFTER the 'Stock not locked today' block (orig L540-543, now drifted). 7-day lookback per store code ['WLMHW','HEMW']: skip if closingLocked===true (legacy locked days exempt), skip if theftVerified&&theftVerified.by (the day-level object stock's doLockClosing stamps), for back===0 require movementsSubmitted===true (no mid-entry nag), sum movements[*].theft, push {sev:'high',area:'Stock',msg:sc+' theft '+tu+' unit(s) on '+dd+' not SM-verified — verify & lock closing',at:dd}. Own try/catch. VAR-HOIST FIX: the sketch's `var sb`/`var dd` collide with the function-scoped hoists already in buildExceptions (`var sb` inside the L541 forEach, plus q/stm/s/led elsewhere) — blocks run sequentially so reuse is technically benign, but RENAME to tsb/tdd/tback/ttu for safety; keep dMinus as the local date helper (do NOT use cutoffIso — it is UTC). Cost ≤14 L() reads per tick.\n  3c. P1-43 — insert the QMS-cash-vs-statement block AFTER the 'Cash statement today not closed / mismatch' block (orig L544-548), BEFORE the 'Missing vouchers' comment (orig L549). Exactly per sketch: derive (never consume) CASH_CLOSED; CROSS_DAYS=3 LOCAL days, CASH_TOL=1; only s.closed===true days; skip whole day on any paymentMode==='Mixed'; qCash from outcome 'purchase' (case-insensitive) Cash rows on the exitTime||entryTime||walkInTime day axis; cIn = non-void income/Cash ledger rows (== computeDay(cd).cashIn); one-sided qCash>cIn+1; item {sev:'med',area:'Cash',msg:'QMS cash sales ₹X vs statement cash income ₹Y[ (date)] — figures don\\'t tally; verify billing / statement entries',at:cd} with en-IN rupee formatting. Var names stq/ledq/qq/sD/cd/cdD/qCash/qMixed/cIn as spec'd (avoid the s/q/led/stm hoists — verified present at L536/545/550). COPY BAN (forever): msg must never contain the substring 'mismatch' in any casing — buildCloseDaySteps (index.html L3084) would block EOD step 4.\n\n4. No shell edits anywhere: EXC_AREA_TO_MODULE (index.html L3013) already maps QMS→qms, Stock→stock, Cash→expense, so all three new item areas get working Fix buttons.\n\n5. Verify: P1-8 checks 1/6/7 (salesCount contract, re-submit recompute, Helios attribution), P1-7 check 7 (theft flag appears/clears; closingLocked-legacy and today-unsubmitted produce NO flag), P1-40 V1-V10, P1-43 V1-V10, plus the Wave-8 node bridge harness re-run to prove _dsrRollup writes still preserve the 10 new stock fields (P1-5 check 12).",
  "collisions": "1. theftVerified SHAPE (the one real conflict): P1-6 reads movements[brand].theftVerified as a per-row boolean; P1-7 (field owner) defines it as DAY-LEVEL {by:string,at:ISO}|null, and P1-7's bridge block + normalise carry + doLockClosing writer all use the day-level object. RESOLUTION: P1-7 wins (also matches the HANDOFF name list which pairs theftVerified with the P1-7 semantics). P1-6's monCellCalc changes to `theftVerified: !!(day.theftVerified && day.theftVerified.by)` (per-day, applied to every theft row of that day); theftRemark stays per-row. P1-6's harness seed for 2026-06-10/15 moves theftVerified to the day-blob top level.\n\n2. Duplicate actor helper: P1-5's stActor() and P1-7's smName() both do a read-only peek at saagar_active_staff_v1. RESOLUTION: implement ONLY stActor() (role fallback 'SM'/'CRO'); P1-7's call sites (doLockClosing theft stamp) are SM-gated so stActor() yields the identical 'SM' fallback. smName() is not written.\n\n3. doLockClosing (P1-5 + P1-7, verified L1946-1981): composed order = roBlock → SM gate → fresh loadData → missing-gap guard (L1955-58) → P1-7 theft PRE-CHECK (may set theftVerified, or return before any mutation) → gapRemarks loop → closingSignoff (untouched) → P1-5 closingLockedBy/At stamps → closingLocked=true (L1975) → single existing saveData (L1976). No line overlap.\n\n4. submitMovements (P1-5 + P1-7, verified L1994-2004): P1-7 gate between loadData and the flag; P1-5 stamps after the flag. doSubmitTheftRemarks re-enters submitMovements, so stamps land exactly once, on the clean pass.\n\n5. toggleLock (P1-5 only): the restructure preserves the closing-lock detour at L1863, which is all P1-7 depends on. P1-7 makes no toggleLock edit.\n\n6. Summary tab triple touch (P1-5/P1-7/P1-8): markup — DOM order fixed as #sum-cards → #sum-dsr-recon (P1-8) → #sum-audit (P1-5). renderSummary — P1-7's one-liner edits the theft scard sub INSIDE the sum-cards HTML string (L1554); P1-5 appends renderSummaryAudit(data) after that assignment; P1-8 appends renderDsrRecon(data,tSales) last before applyReadOnlyChrome (L1560); BOTH P1-5 and P1-8 add their clear lines to the empty-brands early return (L1479-85). printSummaryReg — order: stp-cards → P1-8 parity line (after orig L2651) → existing tables → P1-5 audit-trail section (immediately before stPrintDoc, orig L2659). P1-6 references the formula only and never enters renderSummary/sec-summary.\n\n7. Modal insertion point 'after L800': P1-5's #stock-reopen-confirm and P1-7's #stock-theft-confirm both anchor there — sequential inserts, id namespaces rop-*/thf-*/clk-* disjoint; P1-7 also adds #clk-theft INSIDE #stock-lock-confirm (between L792/L793), which P1-5 does not touch.\n\n8. Fresh-blob literals: initData() and the movements-row construction sites (initData template, addBrand L2174) get P1-5's 8 fields + P1-7's theftVerified:null + theftRemark:'' in ONE composed edit each.\n\n9. normaliseImportData one-pass (P1-5 + P1-7 + P1-8 constraint): P1-7's theftRemark goes inside the movements rebuild literal (L2262-2268 — the only place a silent field-drop can happen, verified); P1-5's 8 fields + P1-7's theftVerified after the boolean block (L2283-2285); P1-8 contributes NO line but a binding constraint — keep the verified in-place mutation pattern so _dsrRollup(+salesCount) carries implicitly.\n\n10. Insert-after-L1561: P1-6's monthly JS block and P1-8's renderDsrRecon both land after renderSummary's closing brace — sequential, all names disjoint (mon*/renderMonthly vs renderDsrRecon).\n\n11. buildExceptions triple edit (P1-40 replace L535-539 / P1-7 insert after L540-543 / P1-43 insert after L544-548): apply top-to-bottom, anchor by comment strings since P1-40 alone shifts everything below by ~18 lines. Function-scoped var hoists (q L536, sb L541, stm/s L545, led L550) — P1-7's block renames sb/dd/back/tu → tsb/tdd/tback/ttu; P1-43's names already chosen to avoid them; P1-40 reuses q by design (same try).\n\n12. P1-6's own note 'apply P1-6 after P1-8' is OVERRIDDEN by the mandated order P1-5→P1-7→P1-6→P1-8: safe because the regions are structurally disjoint (P1-6 = tab nav L478, goTab L2079, new section between L662/664, new code after L1561/L2660; P1-8 = sec-summary interior + renderSummary interior + printSummaryReg interior) — only line numbers drift, hence the anchor-by-content rule.",
  "contracts": "AUTHORITATIVE cross-file contract set (reconciled):\n\nA. Stock day blob key: saagar_stock_<titanworld|helios>_<YYYY-MM-DD> (SK_PREFIX L820, dataKey L941; bridge reaches it via skey(): WLMHW→titanworld, HEMW→helios).\n\nB. Stock-authored fields (stock.html sole writer):\n- Top-level: openingLockedBy/openingLockedAt, closingLockedBy/closingLockedAt (written by toggleLock lock-leg / doLockClosing), movementsSubmittedBy/movementsSubmittedAt (submitMovements; overwritten on re-submit), reopenedCount (int, doReopenConfirm), reopenLog (array of {section,by,role,at,reason}, cap 50), theftVerified ({by,at}|null — SOLE writer doLockClosing; nulled ONLY by updMov when a theft figure changes; reopen without a theft edit keeps the stamp).\n- Movements row: theftRemark (string, written by doSubmitTheftRemarks).\n\nC. Bridge-authored field inside the stock blob: _dsrRollup.salesCount (integer >= 0; absent = no data). Written ONLY by consumeDsrToStock at the L329 rollup write, recomputed in full each write from ALL of today's submitted saagar_dsr_<date>_* records for the store (sales[] rows with type!=='service' && Number(amount)>0; store via dsrStoreCode: record store code else employee-master 'helios'/'titan' text match else WLMHW — the SAME resolver now also picks the agg bucket at L322 so count and rollup agree). Valid as of _dsrRollup.at; P1-12 :v<rev> re-submits trigger a fresh recompute (never incremented). Readers (renderDsrRecon, printSummaryReg parity line) MUST hide on missing/non-finite/negative Number(salesCount). The bridge's load-mutate-save at L328-330 preserves all field-B additions (verified pattern; re-proven by the node harness in verify).\n\nD. Bridge READS of the stock blob (buildExceptions P1-7 block, all read-only): closingLocked (true ⇒ skip — legacy exemption), theftVerified.by (truthy ⇒ skip), movements[*].theft (summed), movementsSubmitted (today-only gate). The bridge NEVER writes the stock blob from buildExceptions.\n\nE. saagar_exceptions: bridge-owned, rebuilt wholesale each cycle, blob {date,items,generatedAt} unchanged, item shape exactly {sev:'high'|'med'|'low', area:string, msg:string, at:'YYYY-MM-DD'}. New items: P1-7 {sev:'high',area:'Stock',msg:'<CODE> theft N unit(s) on <date> not SM-verified — verify & lock closing'}; P1-40 {sev:'med',area:'QMS',msg:'N follow-up(s) due[ (M overdue)] — see Follow-ups'}; P1-43 {sev:'med',area:'Cash',msg:'QMS cash sales ₹X vs statement cash income ₹Y[ (date)] — figures don\\'t tally; verify billing / statement entries'}. Routing is derived from area via EXC_AREA_TO_MODULE (index.html L3013 — QMS/Stock/Cash all pre-mapped, zero shell edits). PERMANENT COPY BANS: area-QMS msgs never match /open lead/i (EOD wizard L3070); area-Cash msgs never contain 'mismatch' (EOD wizard L3084).\n\nF. Shell key saagar_active_staff_v1 ({id,name,employeeId,at}, written by shell signInStaff): READ-ONLY peek by stock's stActor(); fallback role string 'SM'/'CRO'. Stock never writes it.\n\nG. Other bridge read-only inputs: retail_queue_management_v1 → followups[].{status,dueDate} (P1-40) and customers[].{outcome,paymentMode,purchaseAmount,amount,exitTime,entryTime,walkInTime} (P1-43); gm_expenses rows {date,type,mode,amount,void} (P1-43); tanishq_statements[date].closed (P1-43); saagar_employee_master_v1 name/store/active (P1-8). None ever written.",
  "open_questions": [
    "Semantic side-effect of the theftVerified reconciliation: verification is per-DAY (P1-7 stamps one {by,at} per day blob), so P1-6's Monthly theft log will show '✓ SM verified' on EVERY theft row of a verified day, even when multiple brands had theft that day. This is faithful to how the stamp actually works, but the P1-6 spec's column implied per-row granularity — confirm the day-level display is acceptable (recommended: yes; per-row verification would require a schema change P1-7 deliberately avoided).",
    "P1-5 extends the mandatory-reason re-open sheet to the OPENING unlock as well (previously a silent SM flip; the register text is quoted only for closing/movements re-open). The spec asserts this is register-mandated — confirm opening-unlock friction is intended before shipping, since it changes a daily SM gesture.",
    "Demo-APK optics: P1-7's HIGH-severity theft item (7-day lookback) and P1-43's cash item can both fire on seeded demo data (P1-43 edge 17 accepts this as illustrative; the seed also creates unlocked past days). Decide whether the Wave-9 seeded APK should tune demo-seed.js to avoid red flags on first open, or keep them as demo material (no seed edit is in any spec's scope)."
  ]
}