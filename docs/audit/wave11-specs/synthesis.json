{
  "carry_plans": [
    "grooming.html — VERIFIED NO NORMALISER (getDay L544 is raw JSON.parse(...||'[]'); saveRecord L537 parse+push+stringify passes old records through byte-identical; deleteRecord L548 splice; clearDayData L554 remove). New persisted fields, all OPTIONAL flat fields on records inside saagar_grooming_YYYY-MM-DD arrays, written ONLY in the single save path saveCRO() (record literal L765-770, sole saveRecord caller L772): (1) store: string 'WLMHW'|'HEMW'|'' — default absent on legacy, '' when master lookup fails; (2) empId: string — OMITTED entirely (conditional assign, never null) when the matched master row has no id (shell normalizeEmployee at index.html L2336-2350 gives master rows both .id (uid) and .employeeId — spec stamps .id); (3) checkedBy: string ≤60; (4) checkedByRole: string ≤40; (5) checkedAt: ISO string — all default absent. Carry sites (no normaliser, so these are READ-SIDE carries): renderDaily L836 spread {...r,_i:i} preserves all five for free; renderMonthly croMap L945-946 is the ONE real drop site — the named-field copy croMap[key].records.push({date,pct,checked,total}) MUST gain store:r.store, and the croMap seed L945 gains store:(r.store==='WLMHW'||r.store==='HEMW')?r.store:'' with ''-to-code upgrade only (never overwrite a resolved code); exportCSV L1038-1041 gains trailing Store / Checked By / Self-Check cells (null-guarded r.store||'' and r.checkedBy||''). Every renderer must null-guard (rec.checkedBy? / grmRecMatches normalising ''-vs-absent) — legacy and old-APK records lack all five fields forever. NO new localStorage keys (grmStoreFilter is in-memory; P1-20 persists nothing).",
    "cro_audit.html — one new persisted field: byStore on the EXISTING key cro_s_v3, sparse map {WLMHW?:{surveys?,reviews?,mktg?}, HEMW?:{...}} (only >0 overrides stored; delete s.byStore when all six inputs blank), default ABSENT ⇒ every reader falls back per-field to globals (zero-config byte-identical). Read carry VERIFIED FREE: getS() L780 = Object.assign({},DEF,parsed) and DEF (L778) has no byStore key, so the stored map wins. WRITE CARRY IS THE ITEM'S CORE TRAP, VERIFIED: saveSettings() L782-795 is the ONLY writer of cro_s_v3 (L792 setItem; only other reference is the L780 read) and it rebuilds a 5-field object literal from form inputs — it MUST become const s={...getS(), surveys:..., npsScore:..., rate:..., reviews:..., mktg:...} then rebuild byStore from the six s-WLMHW-*/s-HEMW-* inputs; without the spread, byStore silently drops on the next 'Save Targets' tap. loadSettingsForm() L797-805 additionally fills the six new inputs and sets live 'Global (n)' placeholders. Audit records (cro_audits_v3, getAudits L1460) untouched — store dimension already exists as a.store ('Titan World, Latur'/'Helios, Latur' from the f-store select L555-559). P1-23 persists NOTHING (prefill flows into existing tData.t5.billsCount via onNpsInput L1223; transient marker lives in inp.dataset.autoFilled). dashStore is in-memory only.",
    "payroll.html blob (P1-48) — new OPTIONAL field statTotals {emp:int, pt, pfEE, pfER, esEE, esER, net:number RAW unrounded, at:ISO} on state.runs['<MonthName>-<Year>'] inside key payroll_suite_v1_2026 (STORAGE_KEY L1367), written ONLY by lockRun() (L3032-3054) by summing the just-built snapshot (calcGM outputs: pt, pfEE, pfER←c.pfEmpr, esEE←c.esicEE, esER←c.esicEmpr, net←c.finalPay — mapping verified against payTotals L2863-2865 and openEngineReport L1472-1474). Carry VERIFIED ZERO-CODE: normalizeState() L1594 does s.runs=(s.runs&&typeof s.runs==='object')?s.runs:{} — run objects pass through untouched (also on JSON import, which routes through the same normaliser); normalizeRow/normalizeAdvance never touch runs; DO NOT add per-run reshaping (month-viewer comment L2894-2895 promises runs are never migrated). unlockRun() L3060 rebuilds the run as {status:'approved',preparedBy,checkedBy,approvedBy} — statTotals drops with the snapshot, INTENTIONAL stale-clear (comment only, no code change); every relock recomputes. Consumers (renderApproval, archiveKeys L2898, gmComputed L1732) read named fields only — extra field invisible. Demo-seed caveat: loadState L1620 reseeds the WHOLE state (runs included) whenever embedded app-data rows exist — same as existing lock behaviour, noted in open questions.",
    "payroll_fnf_v1 (P1-27) — the wave's ONLY new localStorage key: JSON array of F&F records {id:'FNF<ts>', empId, name, designation, period, lastDay, earnings:[{label,amt:int}], recoveries:[{label,amt:int}], totEarn, totRec, net:int, netWords, ref:'GM/FNF/...', generatedAt:ISO, generatedBy, status:'final'}, newest first. Own on-read normaliser fnfList() returns [] on parse-fail/non-array (no existing normaliser touches it). Sibling-key justification VERIFIED: loadState() L1617-1622 — when defaultState() embedded rows exist (demo builds) the entire state is replaced by normalizeState(emb), so anything stored INSIDE state would be wiped every open; precedent ATT_FEED_KEY='saagar_payroll_attendance_feed' at L3175. fnfSave respects ST_READ_ONLY via stGuardWrite (L1413-1416); PDF generation still allowed read-only. Records are write-once; re-print passes stored numbers verbatim.",
    "integration-bridge.js (P1-25) — NO carry: writes only saagar_exceptions (EXC L41), which buildExceptions already rebuilds wholesale each tick and overwrites via S(EXC,{date:d,items:ex,generatedAt}) at L692; derived data, nothing persists to be dropped. Reads only existing fields: cro_audits_v3[i].date/.cro, saagar_employee_master_v1[i].name/.active, saagar_gate_status.unavailable[k].leave, saagar_bridge_config.dsrClosingTime (cfg() L56-61, default '20:30' L60). Cross-file dependency: cro_audit.html owner must not rename/reshape a.date or a.cro (additive rules already guarantee this; P1-23/24 don't touch them).",
    "saagar-report.js — NO storage writes anywhere in the wave's three edits. New private read-only helper _payrollLockedTotals(ym) parses payroll_suite_v1_2026, converts 'YYYY-MM' → '<MonthName>-<Year>' with a hardcoded English MONTHS array (matches payroll MONTHS L1364 / pkey L1687 — never toLocaleString), returns null unless run.status==='locked' && typeof run.statTotals==='object'; fully try/caught. statutoryRegister and fnfSettlement builders are live-opts-only (no localStorage fallback, mirroring statutorySummary L464-469's pattern)."
  ],
  "owner_plans": [
    "OWNER 1 — grooming.html (_extracted_modules/grooming.html, 1129 lines; P1-21 → P1-22 → P1-20 with composed edits; re-embed via scratchpad p1plan/embed.js; tab switcher go('cl'|'daily'|'monthly') L592). PHASE 1 · helpers + state: after grmFailPct() (~L636) add grmEmpRow(name) (raw master reader: first case/trim-insensitive name match in saagar_employee_master_v1, NO active/leave filter, try/catch→null) and grmStoreCode(name) BUILT ON grmEmpRow (var e=grmEmpRow(name); if(!e)return''; st=String(e.store||'').toLowerCase(); 'helios'→'HEMW', 'titan'→'WLMHW', else '' — NO WLMHW fallback, deliberate divergence from bridge dsrStoreCode, comment it); after esc() (L810) add grmActor() (peek saagar_active_staff_v1 → name.slice(0,60), role from saagar_current_role_v1 slice(0,40), both try/caught; fallback name=role||'Staff') and grmIsSelfCheck(rec) (trim+lowercase compare checkedBy vs name); near curGender (L605) add let grmStoreFilter='ALL', plus grmSetStore(v) (sync #grm-store-daily/#grm-store-monthly, call renderDaily()+renderMonthly()) and grmRecMatches(r) (s=(r&&(r.store==='WLMHW'||r.store==='HEMW'))?r.store:''; ALL→true; UNASSIGNED→s===''; else s===filter — never test 'store' in r). PHASE 2 · ONE composed save-site edit (L763-770): before the literal add const __emp=grmEmpRow(name); const __actor=grmActor(); literal becomes {name, gender:g, pct, checked, total, items, date:todayKey(), time:..., attempt:__attempt, store:grmStoreCode(name), checkedBy:__actor.name, checkedByRole:__actor.role, checkedAt:now.toISOString()}; then if(__emp&&__emp.id!=null)record.empId=__emp.id; (both startCL and startRecheck funnel through this one saveCRO — one edit stamps both). PHASE 3 · markup+CSS: Store <select> .fg block in the daily filter-bar (inside L451-457, before Clear) id=grm-store-daily and in the monthly filter-bar (inside L477-483, before Export CSV) id=grm-store-monthly, options All stores/Titan World/Helios/Unassigned; <th>Store</th> after Gender th (L503); P1-20 topfail panel div (id=grm-topfail, .card with sec-title 'Top Failed Checks' + #grm-topfail-sub + #grm-topfail-body, display:none) between statgrid close (L490) and #monthly-empty (L492); CSS block: .tf-* rules per P1-20 + bump .mt min-width 640px→700px (L289 media block). PHASE 4 · renderDaily (L811-886): L813 split const dayAll=getDay(dateStr); const records=dayAll.filter(grmRecMatches); (stats L814-829 use filtered records); L836 sorted chain = [...dayAll].map((r,i)=>({...r,_i:i})).filter(grmRecMatches).sort(...) — FILTER AFTER the _i MAP so delDay(dateStr,origIdx) L880 splices the raw-array index; filtered-empty copy (title 'No records for this filter', sub 'Change the Store filter or pick another date' when dayAll.length>0); grmPendingForDate (L640) roster gains .filter(e=>grmStoreFilter==='ALL'||grmStoreCode(e.name)===(grmStoreFilter==='UNASSIGNED'?'':grmStoreFilter)); COMPOSED card sub-line (L873, one edit for P1-21+P1-22): `${rec.gender==='m'?'Male':'Female'} CRO &nbsp;·&nbsp; ${rec.time}${rec.store==='HEMW'?' · Helios':rec.store==='WLMHW'?' · Titan World':''}${chkLine}` where chkLine = rec.checkedBy ? ' &nbsp;·&nbsp; checked by '+esc(rec.checkedBy)+(grmIsSelfCheck(rec)?' <span style=\"color:#b45309;font-weight:600\" title=\"Checker name matches the employee being checked\">⚠ self-checked</span>':'') : '' — esc() on checkedBy MANDATORY; do NOT print checkedAt on the card. PHASE 5 · renderMonthly (L934-1030), three sub-edits in one pass: (a) first line inside recs.forEach (L943): if(!grmRecMatches(r))return; (b) croMap seed L945 gains store field + else-if ''-upgrade; records.push L946 gains store:r.store (THE carry trap); row template L1018 gains Store <td> (Helios/Titan World/—) after the gender td (sticky cols 1-2 CSS L193-196 unaffected); (c) append P1-20's try/catch Top-Failed block at the function tail (after L1029 loop, before L1030 close) EXACTLY per its spec BUT with one synthesis mandate: add if(!grmRecMatches(r))return; as the first line of its per-record loop so the panel agrees with the active store filter (P1-20 was spec'd pre-filter); reuse in-scope allKeys/empty/getDay; esc() on labels; hide panel when empty||top.length===0. PHASE 6 · exportCSV (L1035-1050), ONE composed edit superseding all three specs: header ['Date','CRO Name','Gender','Score %','Checked','Total','Time','Store','Checked By','Self-Check']; per-record: skip if !grmRecMatches(r); cells ..., r.time, (r.store||''), q(r.checkedBy||''), grmIsSelfCheck(r)?'YES':'' — add a tiny local q(v) CSV-quoter for the Checked By cell ONLY (existing exportCSV has NO quoting; leave legacy columns byte-identical). VERIFY: run P1-21 steps 1-6, P1-22 steps 1-9, P1-20 steps 1-10, with P1-20 step 8's 'exportCSV byte-identical' replaced by 'legacy 7 columns unchanged under All-stores filter'; index-safety delete-under-filter check is mandatory; past-date st-date read-only regression; 0 console errors across go('cl'/'daily'/'monthly').",
    "OWNER 2 — cro_audit.html (_extracted_modules/cro_audit.html, 2254 lines; P1-23 → P1-24; tab switcher is go('audit'|'dash'|'history'|'settings') at L1916-1925 — NOTE both specs mis-cite it: P1-23 says go('home'), P1-25 says go('dashboard'); use the real ids). PHASE 1 · P1-23 prefill: after setGroomNote (L2047-2058) add setBillsNote(kind,text) (note div appended inside the #nps-bills .mr cell, reuse .groom-note auto/manual CSS L150-153); after autoFillGrooming (L2059-2083) add autoFillBills() exactly per spec (editable — never readOnly; untouched = value===''||dataset.autoFilled==='1'; feed hit → Math.round(Number(fe.qmsSales)||0), dataset.autoFilled='1', note '✓ auto from QMS — edit if register differs', onNpsInput(); feed miss + flag → stale-clear value+flag+note+onNpsInput; manual value → hands off) and npsBillsTouched() (clear flag+note); buildNpsBody L1063 input oninput becomes \"npsBillsTouched();onNpsInput()\"; loadAuditForEdit: after setV('nps-bills',...) L2182 add const nb=document.getElementById('nps-bills'); if(nb)nb.dataset.autoFilled=''; if(typeof setBillsNote==='function')setBillsNote('',''); (mirrors the grooming reset at L2188-2189). PHASE 2 · P1-24 settings core: after DEF (L778) add CRO_STORES, croStoreCode(label) ('titan'→WLMHW,'helios'→HEMW, else null), getTargets(storeCode) (type-guarded byStore/per-store object; +o.x||global per field; npsScore/rate ALWAYS global), getDashTargets() (dashStore→getTargets(dashStore); no byStore→getTargets(null); configured-All→sum of the two per-store effective volume targets, rate/npsScore global); REWRITE saveSettings (L782-795) with const prev=getS(); const s={...prev, five scalars...}; rebuild byStore from the six inputs (v>0 gate), if(Object.keys(byStore).length)s.byStore=byStore; else delete s.byStore; keep the stIsPast() guard first; loadSettingsForm (L797-805) also fills s-WLMHW-*/s-HEMW-* from getS().byStore (blank when absent) and sets placeholder='Global ('+s.surveys+')' etc live; insert the 'Per-Store Targets (optional)' card in sec-settings after the Monthly KPI Targets card / before Scoring Reference (~L691) with the two labelled trios per spec. PHASE 3 · P1-24 dashboard: let dashStore=''; next to dashMode/dashOff (L829-830); store-toggle markup ABOVE the existing period-toggle (L612) — All Stores/Titan World/Helios buttons + setDashStore(c) (toggle .active, renderDash()); renderWeekDash: L1607-1608 const→let cur/prv then if(dashStore){const f=a=>croStoreCode(a.store)===dashStore; cur=cur.filter(f); prv=prv.filter(f);} ; add const T=getDashTargets(); swap s.surveys/s.reviews/s.mktg→T.* at L1610 wT_* (keep s for rate/npsScore); renderMonthDash: same let+filter at L1695-1696, T swaps at KPI cards L1723/L1738/L1743-1745 and per-CRO denominators L1767/L1771; buildWeekBreakdown call L1719 → buildWeekBreakdown(cur, mi.year, mi.month, {...s,...T}) (its wT_* L1800 and header L1821 then follow — verified signature L1784); empty-states L1617/L1704 append ' — '+store short name when filtered; optional #pn-sub suffix (L1613/L1700). PHASE 4 · P1-24 entry form: onNpsInput L1215, onRevInput L1239, onMktgInput L1252: const s=getS() → const s=getTargets(croStoreCode((document.getElementById('f-store')||{}).value)); (getTargets returns the same 5 field names so calcNps/calcRev/calcMktg L868-875, wkly/daily and updWM need zero further edits); loadPreData (L842-863): const→let wAudits/mAudits, then byStore-gated store scoping per spec (only filters when getS().byStore exists AND a store is selected — preserves today's both-store pre-counts otherwise). PHASE 5 · COMPOSED onCroDateChange (L2123-2131) — the P1-23/P1-24 collision point, final body order: loadPreData(); autoFillGrooming(); autoFillBills(); renderCroContext(); updDupBanner(); existing T6 guard (rev-buyers → croRenderReviewRows()+croReviewDerive()); then element-existence-guarded if(document.getElementById('nps-bills'))onNpsInput(); if(document.getElementById('rev-cnt'))onRevInput(); if(document.getElementById('mktg-cnt'))onMktgInput(); — the tail re-scores refresh the wm-progress bars after a store switch; note loadAuditForEdit's own L2190-2193 recompute runs AFTER setV so saved values win (P1-23's flag reset prevents later clobber). VERIFY: P1-23 steps 1-7 (feed seed shape {'<date>':{'<kk(cro)>':{qmsSales:...}}} per readCroFeed L2008/croFeedEntry L2009-2014) + P1-24 steps 1-7 (carry check #4 is mandatory: re-save with untouched per-store inputs must preserve byStore; blank-all must delete it) + grooming-autofill regression (#groom-pct still read-only auto) + full 4-tab sweep 0 console errors.",
    "OWNER 3 — payroll.html (_extracted_modules/payroll.html; P1-48 → P1-26 → P1-27; GOTCHA: the <script id=\"app-data\" type=\"application/json\"> seed block false-fails naive JS syntax checks — lint only real script blocks; tab switcher switchTab(tab) L1807, tabs incl 'approval','reports','hr'). PHASE 1 · P1-48 lock write (lockRun L3032-3054): immediately after the snapshot loop (L3043) insert the statTotals sum — const statTotals={emp:0,pt:0,pfEE:0,pfER:0,esEE:0,esER:0,net:0,at:new Date().toISOString()}; Object.keys(snapshot).forEach(kk2=>{const c=snapshot[kk2]; statTotals.emp++; statTotals.pt+=safeN(c.pt); statTotals.pfEE+=safeN(c.pfEE); statTotals.pfER+=safeN(c.pfEmpr); statTotals.esEE+=safeN(c.esicEE); statTotals.esER+=safeN(c.esicEmpr); statTotals.net+=safeN(c.finalPay);}); (avoid shadowing — don't name the loop var 'kk'... actually payroll has no kk; still prefer a distinct name); add statTotals to the run literal L3045-3051 ({...snapshot, advRecovered, statTotals}); store RAW unrounded sums; add the one-line comment in unlockRun (after L3060): '// statTotals intentionally dropped with the snapshot — a stale statutory sheet must never outlive an unlock (P1-48).'; optional L3053 toast extension '...Statutory totals saved for the Month-end pack.'; NO other payroll edit for P1-48 (queueSave L3052 persists; emp-count===snapshot-key-count guaranteed by runBlockingErrors L2868-2891 dup/blank-key block). PHASE 2 · P1-26 member-wise register: insert after reportStatutoryPdf (L3133): statutoryMemberRows() (one shared extractor over payTotals() rows — esicWages derived as Math.round(safeN(c.grossPayable)-safeN(c.washing)) ONLY when esicOn (c.esicEE>0||c.esicEmpr>0), else 0 — calcGM L1772-1799 verified to expose pfWages/washing/pfEmpr/esicEmpr but NOT esicWages; row master fields r.uan/r.esicIp verified in normalizeRow L1570; whole-rupee Math.round matching reportStatutoryCsv L3127-3129), reportStatutoryMemberCsv() (header 'Sr,UAN,Employee Name,Emp ID,Gross Payable,PF Wages,PF EE,PF ER,ESIC IP No,ESIC Wages,ESIC EE,ESIC ER,PT'; leading-apostrophe guard on UAN and ESIC-IP cells = reportBankCsv accountNo convention L3121; q() quoting on name/empId; TOTAL row from T-based totals, do NOT re-sum rounded rows; appSaveBlob L2257 filename StatutoryRegister_<period>.csv), reportStatutoryMemberPdf(){openEngineReport('statutoryRegister');}; openEngineReport (L1465-1486): convert L1472-1481 if/else to if(kind==='statutorySummary'){...existing...}else if(kind==='statutoryRegister'){const sm=statutoryMemberRows(); opts={period:repPeriod(),locked,preparedBy:m.preparedBy||'',approvedBy:m.signatory||'Authorised Signatory',rows:sm.list,totals:sm.tot};}else{...existing payrollRegister...}; tail L1482-1484 unchanged (posts ST_REPORT — shell handler at index.html L6729 verified reportType-agnostic, NO shell edit); Reports-tab Statutory card body rewrite L1166-1171 per spec (retitle 'Statutory — Summary & Register', keep existing two buttons' onclicks verbatim, add Member-wise PDF/CSV buttons). PHASE 3 · P1-27 F&F: append the fnf-card markup inside pane-hr (L1055+, below the existing HR-letter card); add functions near the HR block (~after hrWhatsApp L2842): fnfList/fnfSave (sibling key payroll_fnf_v1; ST_READ_ONLY→stGuardWrite blocks save, PDF still opens), fnfAdvOutstandingFor(row)=Math.round(Σ advOutstanding(a) over (state.advances||[]).filter(a=>advMatch(a,row))) (advMatch L1696, advOutstanding L1712), fnfAssemble(gi) per spec — build from COMPONENTS (earnings: Math.round(safeN(c.grossPayable)) + OT line when safeN(c.otAmount)>0; recoveries: pt/pfEE/esicEE lines when >0 + single 'Advance outstanding (all vouchers, full recovery)' line when advOut>0; NEVER c.advance/c.finalPay — EMI double-count trap) — IMPLEMENTATION FIX: the spec sketch's fmtLD()/fmtOt() helpers DO NOT EXIST; use the existing fmtN() (L1443): salary label '...('+fmtN(c.totalSalaryDays!=null?c.totalSalaryDays:(c.finalPresent||0))+' salary days)', OT label 'Overtime ('+fmtN(c.otDays||0)+' OT day(s))'; 4 manual +/- row slots (blank label or 0 amt ignored; 5th add no-ops with toast 'Up to 4 adjustment rows.'); fnfGenerate (guards: employee selected; A.c.totalDays>0; record shape per carry plan; netWords via slipWords(Math.abs(net)) L2327; ref GM/FNF/<year>/<id8>), fnfOpenPdf(rec) (postMessage {type:'ST_REPORT',reportType:'fnfSettlement',opts:{fnf:rec}} → SaagarReport.preview fallback → toast), renderFnfHistory (stored-record re-print verbatim, slipEsc L2326 on every interpolation), renderFnfPreview + live #fnfNet; append to renderHr() (L2727-2733, keep existing lines intact): populate #fnfEmp from ALL gmRows() (L1933 — retired rows included; ' — retired' suffix when row.active===false; option value = gmGlobalIdx(li) pattern from L2729), non-blocking still-Active warning line, fill #fnfPeriod from state.meta.month+' '+state.meta.year, default #fnfLastDay stRealToday() (L1409), renderFnfHistory(). VERIFY: P1-48 steps 1-8 (incl. payTotals().T cross-check and unlock-drops-statTotals), P1-26 steps 1-8 (stub appSaveBlob + parent.postMessage in-frame), P1-27 steps 1-9 (advance-outstanding 8000 fixture; re-print immutability; corrupt payroll_fnf_v1→[]), plus normalizeState pre/post identity and 0 console errors across all 9 tabs.",
    "OWNER 4 — www/integration-bridge.js (canonical V:\\Co work\\Projects\\Retail\\saagar-control-centre\\www\\integration-bridge.js; P1-25 only — no sibling touches this file). ONE additive block inside buildExceptions (L561-694): insert AFTER the Wave-4 DSR-unsubmitted block (ends L690, its closing }catch(e){} ) and BEFORE ex.sort (L691), exactly the spec's sketch: own try/catch; evening gate __cn>=cfg().dsrClosingTime (same zero-padded HH:MM string-compare idiom as L682-684 — REUSED knob, no new config field); eligibility = employee master rows (EMP_MASTER L28 via L() L65) with e&&e.name&&e.active!==false, minus gate.unavailable[kk(name)].leave===true (GATE L29) — the EXACT pair the existing grooming block L567-569 mirrors and cro_audit's croMasterOptions (L2027-2033) + leaveBlockedSet (L2016-2021) define; done-set = cro_audits_v3 records with a.date===d (business date, NOT submittedAt) keyed by kk(a.cro) (matches module findDupAudit L1464-1468); skip when __celig===0; push ONE aggregate item {sev:'med', area:'CRO', msg:__cp.length+' CRO audit(s) pending today — '+first3.join(', ')+(rest?' +N more':'')+' (after '+__ct+')', at:d} — shape matches the binding {sev,area,msg,at} contract; no dedup state needed (S(EXC,...) L692 overwrites wholesale, same argument as the P1-40 comment L580-584). EOD-safety is PROVEN, not assumed: buildCloseDaySteps (index.html L3058-3092) consumes only areas QMS (/open lead/i L3070), Stock (/closing not locked/i L3082), Cash (/mismatch/i L3087) — area 'CRO' is consumed by NO step, and the msg contains none of the three banned substrings; precedent: the existing area-'DSR' item (L689) is likewise wizard-invisible and hub-generic. Optionally note the __proto__-key exposure comment per spec edge 10 (house pattern, no fix). VERIFY per P1-25 steps 1-7 from the SHELL window (SaagarBridge.runNow(); bridge is not an iframe): leave/inactive exclusion, kk whitespace/case match clears the item, pre-threshold absent, zero-eligible absent, corrupt-blob degrades to item-absent, close-day wizard still exactly 4 steps, QMS/Stock regression items unchanged.",
    "OWNER 5 — www/saagar-report.js (canonical V:\\Co work\\Projects\\Retail\\saagar-control-centre\\www\\saagar-report.js — the saagar-control-centre-demo copy is STALE, do not edit it; NOTE P1-48's saagar-report line numbers were from the stale copy: real anchors are statutorySummary L464-494, hrLetter L563-574, META L911-933, buildModel L1298, _packTypes L1504, pack() L1505-1528, forModule L1593, hub GROUPS/TAGS L1598-1613). PHASE 1 · P1-48 read side: add private helper _payrollLockedTotals(ym) (place next to buildModel ~L1297, inside the IIFE) per the P1-48 contract sketch — hardcoded English MONTHS array, key=MONTHS[mm-1]+'-'+Number(yyyy), stored=JSON.parse(localStorage.getItem('payroll_suite_v1_2026')||'{}'), return {totals:run.statTotals, preparedBy:run.preparedBy||'', approvedBy:run.approvedBy||'', period:key.replace('-',' ')} ONLY when run.status==='locked'&&run.statTotals is an object, else null, fully try/caught; WIRE it as a fallback inside statutorySummary (L464-469): after var t=o.totals||null; add if(!t){var fb=_payrollLockedTotals(o.month); if(fb){t=fb.totals; period=period||fb.period; o.preparedBy=o.preparedBy||fb.preparedBy; o.approvedBy=o.approvedBy||fb.approvedBy;}} — statTotals field names (emp,pt,pfEE,pfER,esEE,esER,net) are exactly the o.totals names read at L470, so the rest of the builder is untouched; helper-null keeps the L469 empty-note; ADD 'statutorySummary' to _packTypes.monthly (L1504, after 'payrollRegister') — this is the synthesis resolution of the P1-48-vs-P1-26 pack ambiguity: pack() (L1521) calls self.build(t,{date:d,month:m}) with m='YYYY-MM' from #rptMonth, which feeds o.month to the fallback; a never-locked month renders the existing empty-note page in the pack (accepted per P1-48 edge 3). Live payroll path (openEngineReport posting opts.totals) is value-identical. PHASE 2 · P1-26: insert the statutoryRegister builder between statutorySummary's closing '},' (L494) and the qmsEodSummary comment (L496), exactly per the P1-26 sketch (landscape; live-opts-only; empty-state 'Open the Payroll module → Reports tab to generate the member-wise statutory register.'; kpi row; 11-col table with raw[] zero-padded for non-money cols per the payrollRegister L410-413 convention; money:[3,4,5,7,8,9,10]; T-based foot; ECR-order note; sign boxes); META entry after L921: statutoryRegister:{title:'Payroll — Statutory Register (Member-wise)',scope:'monthly',icon:'🏛️'}. PHASE 3 · P1-27: insert the fnfSettlement builder between hrLetter's close (L574) and leaveRegister (L576), exactly per the P1-27 sketch (portrait; o.fnf; ref/date note; kv pairs; 4-col Earnings/Recoveries table padded to max(E,R) with raw zero-fill; netbox flips to 'Net Recoverable from Employee' + abs value when net<0; in-words note; settlement para; sign boxes); META entry after L923 (hrLetter): fnfSettlement:{title:'Payroll — Full & Final Settlement',scope:'monthly',icon:'🤝'}. DO NOT add statutoryRegister or fnfSettlement to _packTypes (L1504), forModule (L1593), or hub GROUPS/TAGS (L1598-1613) — verified statutorySummary itself is deliberately absent from GROUPS/TAGS; mirror that (META alone provides preview title + history log). All three phases are independent additions except the statutorySummary in-place fallback — do Phase 1 first and regression-test the live path (openEngineReport('statutorySummary')) before Phases 2-3. VERIFY: SaagarReport.list()/preview empty-state + fixture checks per P1-26 step 6 and P1-27 step 7; _payrollLockedTotals dry-run per P1-48 step 7; Month-end pack builds one extra page (locked month → real totals, unlocked → empty-note) and pack .catch(next) (L1525) shields the loop; existing 6 monthly pack reports unchanged."
  ],
  "collisions": "\"(1) GROOMING saveCRO record literal L765-770 — P1-21 (store, conditional empId) + P1-22 (checkedBy, checkedByRole, checkedAt) touch the SAME literal: land as ONE composed edit (two consts __emp/__actor above it, one literal, one conditional empId assign). Duplicate-helper mandate: ONE raw master reader grmEmpRow() and grmStoreCode() built ON it (not a second master scan); ONE actor helper grmActor() reading saagar_active_staff_v1 (+saagar_current_role_v1 role fallback — verified the staff object has NO role field, shell L2197). (2) GROOMING renderDaily card sub-line L873 — P1-21 store suffix + P1-22 checked-by line: ONE composed template (order: gender · time · store · checked by · ⚠ self-checked). (3) GROOMING exportCSV L1035-1050 — THREE specs disagree: P1-20 says leave untouched ('byte-identical' regression), P1-21 filters rows + appends Store, P1-22 appends Checked By/Self-Check. RESOLUTION: P1-20's freeze applies only to its own item; composed final = legacy 7 columns byte-identical + grmRecMatches row filter + 3 trailing columns (Store, Checked By quoted via a new local q(), Self-Check YES/blank); P1-20's verify assertion is downgraded to 'legacy columns unchanged under All'. (4) GROOMING renderMonthly L934-1030 — P1-21 (filter in forEach L943 + croMap store seed/carry L945-946 + Store column) and P1-20 (appended Top-Failed block): apply P1-21 first; SYNTHESIS MANDATE: P1-20's aggregation loop also applies grmRecMatches so the panel matches the store-filtered stat cards/table (P1-20 was written before the filter existed); P1-20's 'empty' flag is the post-filter cros.length===0 — correct behaviour (panel hides when the filter empties the month). (5) CRO_AUDIT onCroDateChange L2123-2131 — P1-23 adds autoFillBills(), P1-24 adds loadPreData() first + tail re-scores: composed final order loadPreData → autoFillGrooming → autoFillBills → renderCroContext → updDupBanner → T6 guard → element-guarded onNpsInput/onRevInput/onMktgInput; the tail onNpsInput re-run after autoFillBills is an idempotent recompute (safe); in loadAuditForEdit the L2190-2193 handler calls run after setV so saved values win, and P1-23's dataset.autoFilled reset prevents later clobber. (6) CRO_AUDIT onNpsInput L1211-1232 — P1-23 relies on it for t5 scoring, P1-24 rewrites its target source to getTargets(croStoreCode(f-store)): compatible because getTargets returns the same 5 field names; P1-23's autoFillBills needs no change. (7) CRO_AUDIT settings block — P1-24's saveSettings spread-prev rewrite is the single carry-critical edit; P1-23 doesn't touch cro_s_v3 (confirmed). (8) PAYROLL — P1-48 (lockRun L3032-3054), P1-26 (Reports card L1166-1171 + L3115-3133 block + openEngineReport branch L1471-1481), P1-27 (pane-hr + renderHr L2727) touch disjoint regions; single owner applies them P1-48→26→27 with no composed edits needed; openEngineReport's if/else→if/else-if/else conversion is P1-26's alone. (9) SAAGAR-REPORT.JS pack ambiguity — P1-48 says 'adding statutorySummary to _packTypes.monthly belongs to P1-26/27' while P1-26 says 'do NOT add to _packTypes' (that ban is about its OWN new statutoryRegister type): RESOLUTION — the owner ADDS 'statutorySummary' to _packTypes.monthly and wires the _payrollLockedTotals fallback inside the existing statutorySummary builder (otherwise P1-48's stored totals are write-only dead data); statutoryRegister and fnfSettlement stay OUT of _packTypes/forModule/GROUPS/TAGS. (10) SAAGAR-REPORT.JS BUILDERS/META — P1-26 and P1-27 both append entries: independent comma-separated insertions at different anchors (after L494/L921 and after L574/L923), no shared lines. (11) STALE LINE NUMBERS — P1-48's saagar-report citations (L1394/L1395-1418) are from the stale demo copy; canonical anchors are _packTypes L1504 / pack L1505-1528; edit ONLY saagar-control-centre/www/saagar-report.js. (12) TAB-SWITCHER MIS-CITES — cro_audit's real switcher is go('audit'|'dash'|'history'|'settings') L1916 (P1-23's go('home') and P1-25's go('dashboard')/'L521-523' are wrong; harness steps corrected in Owner 2/4 plans). (13) SPEC SKETCH BUG — P1-27's fnfAssemble references non-existent fmtLD()/fmtOt(); substitute fmtN() (payroll L1443) per Owner 3 plan. (14) Bridge P1-25 vs cro_audit owner: read-only dependency on a.date/a.cro field names — additive rules already forbid renaming; no code coordination needed.\"",
  "contracts": "\"CONTRACT A — payroll statTotals (writer payroll.html lockRun ↔ reader saagar-report.js): stored = JSON.parse(localStorage.getItem('payroll_suite_v1_2026')||'{}'); run = stored.runs[key] where key = '<MonthName>-<Year>' built from the pack's 'YYYY-MM' month via the hardcoded English MONTHS array (payroll MONTHS L1364; pkey L1687 → e.g. '2026-07'→'July-2026'; NEVER toLocaleString). run.statTotals = {emp:int, pt:number, pfEE:number, pfER:number, esEE:number, esER:number, net:number (all RAW unrounded sums of the frozen snapshot's calcGM outputs: pt/pfEE/pfEmpr/esicEE/esicEmpr/finalPay), at:ISO-string}. Reader gate: use ONLY when run.status==='locked' && typeof run.statTotals==='object'; else null → existing empty-note (saagar-report.js L469). Reader location: NEW private _payrollLockedTotals(ym) in saagar-report.js (near buildModel L1298), invoked ONLY as the o.totals fallback inside BUILDERS.statutorySummary (L464-469) with opts mapping totals=run.statTotals, period=key.replace('-',' '), preparedBy=run.preparedBy||'', approvedBy=run.approvedBy||'Authorised Signatory'; field names verified identical to the o.totals reads at L470. Pack path: 'statutorySummary' added to _packTypes.monthly (L1504); pack() (L1521) already passes {date,month:'YYYY-MM'} → o.month. Absence semantics: field absent = not locked / pre-Wave-11 lock / unlocked-since (unlockRun L3060 rebuild drops it by design — do not backfill from snapshot). Ship-order safety: writer-without-reader and reader-without-writer both degrade to today's empty-note, never wrong figures. ··· CONTRACT B — P1-26 statutoryRegister (payroll.html ↔ saagar-report.js, live-opts-only): payroll posts window.parent.postMessage({type:'ST_REPORT', reportType:'statutoryRegister', opts},'*') (standalone fallback window.SaagarReport.preview('statutoryRegister',opts)) from openEngineReport's new branch; opts = {period:string(repPeriod()), locked:boolean, preparedBy:string, approvedBy:string, rows:[{sr:int, uan:string, name:string, empId:string, pfWages:int, pfEE:int, pfER:int, esicIp:string, esicWages:int (grossPayable−washing when ESIC applied, else 0), esicEE:int, esicER:int, pt:int, gross:int}], totals:{emp,pfWages,pfEE,pfER,esicWages,esicEE,esicER,pt,gross}} (whole-rupee Math.round; row employer values map from calcGM's pfEmpr/esicEmpr). BUILDERS.statutoryRegister(o) consumes it verbatim, returns {orientation:'landscape', blocks:[header,kpi,table(head/body/raw/money/colStyles/foot),note,sign]}; !Array.isArray(o.rows) → empty-state block (statutorySummary L469 pattern). Shell routing: index.html L6729 ST_REPORT handler is reportType-agnostic (verified) — NO shell edit. NOT added to _packTypes/forModule/GROUPS/TAGS; META entry only. ··· CONTRACT C — P1-27 fnfSettlement (payroll.html ↔ saagar-report.js): payroll posts {type:'ST_REPORT', reportType:'fnfSettlement', opts:{fnf:REC}} (same fallback chain); REC = the payroll_fnf_v1 record shape (id, empId, name, designation, period, lastDay, earnings[]/recoveries[] of {label,amt:int}, totEarn, totRec, net:int (raw sign preserved), netWords, ref, generatedAt, generatedBy, status:'final') — numbers RAW, builder formats via inr(); BUILDERS.fnfSettlement(o) reads o.fnf, returns {orientation:'portrait', blocks:[...]}, netbox label flips to 'Net Recoverable from Employee' with abs value when Number(F.net)<0; !o.fnf → empty-state. META.fnfSettlement={title:'Payroll — Full & Final Settlement',scope:'monthly',icon:'🤝'}; NOT in _packTypes/forModule. Re-print passes the STORED record verbatim (never recomputes). ··· CONTRACT D — P1-25 exception (bridge ↔ shell): item shape exactly {sev:'med', area:'CRO', msg:'<N> CRO audit(s) pending today — <≤3 names>[ +K more] (after <HH:MM>)', at:'YYYY-MM-DD'}; rides the wholesale-rebuilt saagar_exceptions blob (S(EXC,...) L692). Routing: NEW shell mapping 'CRO':'cro_audit' in EXC_AREA_TO_MODULE (index.html L3013; 'cro_audit' is the registry module id used by forModule/navigateToModule) → Home attention card gets a Fix button (L3263-3266 idiom); unmapped interim state degrades to the generic Sync CTA (precedent: existing area 'DSR', bridge L689, is unmapped today) — bridge and shell edits are order-independent. EOD-wizard safety PROOF: buildCloseDaySteps (index.html L3058-3092) consumes hub items only via exIn('QMS').filter(/open lead/i) L3070, exIn('Stock').filter(/closing not locked/i) L3082, exIn('Cash').filter(/mismatch/i) L3087 — area 'CRO' matches no step and the msg copy contains none of the three banned substrings ('mismatch' never appears; item cannot wedge the 4-step wizard). Eligibility predicate mirror: bridge (master active!==false minus gate.unavailable[kk].leave===true) === cro_audit croMasterOptions L2027-2033 minus leaveBlockedSet L2016-2021, so hub count === module datalist. Done-set: a.date===d (business date) matched by kk(a.cro) — same key as module findDupAudit L1464-1468. Evening gate: cfg().dsrClosingTime (bridge L60, default '20:30') — reused knob, no new config. ··· CONTRACT E — store-code attribution rule (per-module resolvers, NO shared helper across blobs): canonical text-match on master/store labels: contains 'helios'→'HEMW', contains 'titan'→'WLMHW'. grooming grmStoreCode: RAW master read (no active/leave filter — a re-check of on-leave staff still stamps), unresolved → '' (Unassigned; deliberately NO WLMHW fallback — mislabeling would corrupt the ranking); stamped at save = point-in-time truth, never re-derived for history. cro_audit croStoreCode: matches the audit record's OWN a.store label ('Titan World, Latur'/'Helios, Latur' from the f-store select L555-559 — no master lookup), unresolved → null → excluded from store-filtered dashboards but always in All. Bridge dsrStoreCode (L201-213, WLMHW fallback, active-filtered) is DSR-specific and stays untouched. ··· CONTRACT F — grooming actor stamp (shell keys, read-only peeks): saagar_active_staff_v1 = {id,name,employeeId,at} (shell signInStaff L2197 — NO role field); saagar_current_role_v1 = plain string (key const L2017); grmActor() = {name: staff.name.slice(0,60) || role || 'Staff', role: role.slice(0,40) || ''}; both peeks try/caught so corrupt shell keys can never throw inside saveCRO.\"",
  "shell_edits": "\"ONE edit, www/index.html L3013 (applied by orchestrator, per P1-25). OLD: const EXC_AREA_TO_MODULE = { 'Floor gate':'grooming', 'QMS':'qms', 'Stock':'stock', 'Cash':'expense', 'Expense':'expense', 'Tax':'tax', 'Service':'service' };  NEW: const EXC_AREA_TO_MODULE = { 'Floor gate':'grooming', 'QMS':'qms', 'Stock':'stock', 'Cash':'expense', 'Expense':'expense', 'Tax':'tax', 'Service':'service', 'CRO':'cro_audit' };  (Exact current literal verified at L3013; module id 'cro_audit' verified against saagar-report forModule L1593 and the shell module registry. Order-independent from the bridge edit — a missing mapping degrades to the generic Sync CTA, never an error. Apply with byte-exact tooling, never PowerShell Get/Set-Content round-trips — UTF-8/emoji mojibake caution.) No other shell edits: the ST_REPORT handler (L6729) is reportType-agnostic so P1-26/P1-27 need nothing, and buildCloseDaySteps is untouched.\"",
  "open_questions": [
    "P1-21 empId source field: shell normalizeEmployee (index.html L2336-2350) gives master rows BOTH .id (internal uid, always present on shell-saved rows) and .employeeId (human 'EMP001' code); the spec stamps record.empId = master.id. Confirm at build time (console-read saagar_employee_master_v1[0]) whether .id exists on THIS device's rows (bridge/demo-seeded rows may lack it) and whether the owner would rather stamp the human employeeId; field is optional-by-design either way (conditional assign, never null).",
    "P1-48 in demo-seed APKs: loadState (payroll.html L1620) replaces the whole state — runs, locks and the new statTotals included — from the embedded seed on every open whenever app-data has rows. Consistent with existing lock behaviour, but a demo of the Month-end-pack statutory fallback requires locking in-session; flag in the changelog/demo script so it isn't mistaken for a statTotals carry bug.",
    "Month-end pack page for never-locked months: with 'statutorySummary' added to _packTypes.monthly, an unlocked/pre-Wave-11 month contributes a PDF page containing only the 'Open the Payroll module…' empty-note (accepted per P1-48 edge 3). Confirm the owner prefers this over skipping the page entirely (skipping would need a pack()-level guard — more invasive; not recommended).",
    "P1-20 panel vs P1-21 store filter: synthesis mandates the Top-Failed aggregation also apply grmRecMatches so the Month-End tab is internally consistent under a store filter (P1-20 was spec'd before the filter existed). If the P1-20 author intended an always-all-stores panel, drop that one added line — everything else is unaffected.",
    "P1-22 CSV quoting: grooming exportCSV (L1035-1050) has NO quoting today (plain join(',')) — names containing commas already break legacy columns. The composed plan quotes ONLY the new Checked By cell to keep legacy columns byte-identical; a fuller CSV-quoting fix is out of scope (candidate for a later hardening pass).",
    "Pre-existing, out of scope but spotted during verification: cro_audit renderMonthDash interpolates the CRO name unescaped (${nm} at L1763) while renderWeekDash escapes it (stEsc(nm) L1669) — a stored-XSS-shaped inconsistency worth a one-line follow-up outside this wave.",
    "P1-24 buildNpsBody/buildRevBody/buildMktgBody static build-time target text (e.g. the initial wm-tag 'Target: N' at L1098-1105) still renders from the s passed by buildTasks (global getS()) until the composed onCroDateChange tail re-scores run — the tail handler calls make the visible bars correct immediately after any store/date change, but if buildTasks itself is ever called without onCroDateChange the first paint shows global targets for a beat. Acceptable; owner may optionally pass getTargets(...) into buildTasks' body builders for cosmetic completeness."
  ]
}